# 《弟弟反击战》MVP 开发实现补充

本补充用于把《游戏开发规划文档.md》从策划描述收敛到当前仓库可以直接开发、测试和扩展的工程实现。

## MVP 范围

当前第一版目标是做出完整可玩的网页原型，而不是一次性完成所有章节的最终美术关卡。

已纳入 MVP：

- Phaser 3 + Vite 网页工程。
- 开始、游戏、失败、胜利四类场景。
- 横版移动、跳跃、下砸、空中滑翔。
- 3 颗心生命、受伤无敌、血量归零哭哭失败。
- 检查点、金币、问号砖、终点旗。
- 姐姐小怪、耳机姐姐、气球姐姐、管道姐姐、Boss 姐姐。
- 随机道具持有 1 个、主动使用、次数和倒计时 HUD。
- 现有角色、道具、转场、音效、音乐、关卡概念图资产接入。

暂不纳入 MVP：

- Tiled 正式碰撞地图。
- 完整剧情演出。
- 存档、选关、星级结算。
- 全部机关的最终物理表现，例如秋千、镜子传送门、购物车轨道。
- 精细 spritesheet 动画。

## 工程结构

```text
index.html
package.json
tsconfig.json
src/
  main.ts
  style.css
  game/
    assetResolver.ts
    GameScene.ts
    levelData.ts
    MenuScenes.ts
    PreloadScene.ts
    types.ts
```

## 资产接入规则

代码优先读取现有 manifest：

- `assets/characters/manifest.json`
- `assets/items/items-manifest.json`
- `assets/transitions/transitions-manifest.json`
- `assets/audio/audio-manifest.json`
- `assets/concepts/levels/level-maps-manifest.json`

为了兼容 Vite build，`src/game/assetResolver.ts` 使用 `import.meta.glob` 把 `assets/` 下的图片和音频转成构建可识别 URL。后续新增资产时保持 manifest 路径即可。

## 核心玩法实现策略

### 关卡

当前 `src/game/levelData.ts` 用 TypeScript 配置描述原型关卡：

- 平台矩形。
- 敌人出生点和巡逻范围。
- 问号砖坐标。
- 固定掉落道具。
- 金币弧线。
- 检查点和终点。

这些配置是 Tiled 前的原型数据。正式关卡进入制作后，可以把同样字段迁移到 JSON Tilemap 的对象层。

### 道具

道具遵循“弟弟同时只能持有 1 个主动道具”的 MVP 规则：

- `uses` 道具按次消耗。
- `durationSeconds` 道具按秒倒计时。
- 捡到新道具会替换当前道具。

当前行为映射：

| 道具 | 当前实现 |
|---|---|
| 火球糖 | 发射直线火球 |
| 玩具锤 | 近距离矩形判定 |
| 跳跳鞋 | 持续时间内提高跳跃高度 |
| 星星披风 | 持续时间内无敌并加速 |
| 泡泡盾 | 抵挡一次伤害 |
| 回旋玩具 | 发射后短暂返回 |
| 爆米花炸弹 | 高伤害抛射物 |
| 飞行帽 | 空中按住跳跃降低下落速度 |
| 冰淇淋枪 | 命中后冻结姐姐 2 秒 |
| 变大饼干 | 临时变大 |

### 敌人

当前敌人共用一个轻量 AI 更新循环：

- 小怪：平台巡逻。
- 耳机姐姐：巡逻并周期释放音波。
- 气球姐姐：关闭重力，上下漂浮。
- 管道姐姐：周期上下探头。
- Boss 姐姐：缓慢移动、扔 projectile，有独立血量。

## 后续优化优先级

1. 用 Tiled 替换 `levelData.ts` 的手写平台。
2. 给角色拆 spritesheet，补跑、跳、受伤、哭哭、胜利动画。
3. 增加 `SaveManager`，保存已解锁关卡和音量设置。
4. 增加正式结算界面：金币、时间、击败数、哭哭次数、星级。
5. 把每个章节的特色机关从配置驱动扩展为独立 controller。
