# 《弟弟反击战》游戏升级开发文档

> 在《游戏开发规划文档.md》和《游戏开发实现补充.md》基础上,做一次**全面系统性升级**。
> v1 侧重内容层(情绪、合成、剧情); **v2 增补基础体验层**(操作、画面、难度叠加、核心循环)——解决"普通、粗糙、不好玩"的根因。
> 本文档为开发落地导向,包含状态机、数据结构、UI 草图、实现步骤和里程碑。

---

## 目录

1. [升级总览](#1-升级总览)
2. [设计原则](#2-设计原则)
3. [互动性升级](#3-互动性升级)
   - 3.1 姐姐情绪系统
   - 3.2 场景可互动对象
   - 3.3 道具合成系统
   - 3.4 弟弟的"反击"机制
4. [可玩性升级](#4-可玩性升级)
   - 4.1 隐藏层与支线房间
   - 4.2 动态难度与"姐姐懂你"
   - 4.3 每日 / 每周挑战
   - 4.4 难度滑块
   - 4.5 收藏图鉴系统
5. [情感剧情升级](#5-情感剧情升级)
   - 5.1 关卡间小剧场
   - 5.2 姐姐的真心话
   - 5.3 弟弟的内心独白
   - 5.4 故事线骨架
6. [技术实现](#6-技术实现)
   - 6.1 工程结构扩展
   - 6.2 状态机
   - 6.3 数据结构
   - 6.4 UI 草图
   - 6.5 资产扩展
7. [开发里程碑](#7-开发里程碑)
8. [附录](#8-附录)
9. [基础体验全面重建（v2 增补）](#9-基础体验全面重建v2-增补)
   - 9.1 现状缺口诊断
   - 9.2 操作手感精修
   - 9.3 画面与视觉表现升级
   - 9.4 难度叠加系统
   - 9.5 核心战斗与爽感循环
   - 9.6 关卡节奏与机制密度
   - 9.7 Juice 与反馈系统
   - 9.8 修订后的总优先级

---

## 1. 升级总览

### 1.1 升级目标

把《弟弟反击战》从**"刚刚能用的 MVP 原型"**升级为**"手感扎实、画面可爱、难度有层次、想一直玩的小品游戏"**。

| 维度 | 当前 MVP 真实状态 | 升级后目标 |
|---|---|---|
| **操作手感** | 能跳能走,空中控制弱,下砸/道具反馈薄,触屏凑合 | 平台动作级手感:可变跳跃、精准落地、清晰输入响应 |
| **画面表现** | 平台/子弹/金币多为代码生成的色块矩形,背景一张长图 | 分层视差 + 主题 tile + 统一光照/描边 + 动效粒子 |
| **难度设计** | 仅 `levelIndex` 线性调投射物参数,Boss 加血 | **局内威胁叠加 + 关卡段压力曲线 + 单局 escalation** |
| **好不好玩** | 10 关结构雷同:跑→跳→打→Boss,缺少"爽点时刻" | 连击/评分/危机波次/道具博弈,每 15 秒一个刺激 |
| 互动性 | 姐姐是木桩 | 情绪反应 + 场景互动 + 道具合成 |
| 情感 | 无故事 | 小剧场 + 真心话 + 独白 |
| 重玩动力 | 几乎无 | 图鉴 + 每日挑战 + 星级评价 |

### 1.2 升级项优先级（v2 修订）

**原则:先修"好不好玩",再加"好不好看",最后加"好不好记"。**

| 优先级 | 升级项 | 投入 | 反馈强度 | 说明 |
|---|---|---|---|---|
| 🔴 **P0-A** | **操作手感精修** | 中 | **极高** | 不修这个,加再多内容也像 demo |
| 🔴 **P0-A** | **Juice / 打击反馈** | 中 | **极高** | 震屏、顿帧、粒子、音效分层 |
| 🔴 **P0-A** | **难度叠加系统** | 中 | **极高** | 局内威胁层 + 压力波次,非仅调数值 |
| 🔴 **P0-A** | **平台/场景视觉替换** | 大 | **高** | 去掉色块矩形,上 tile + 视差 |
| 🟠 **P0-B** | SaveManager + 星级结算 | 小 | 中 | 支撑重玩 |
| 🟠 **P0-B** | 连击 / 评分 / 危机波次 | 中 | 高 | 让"打"有爽感 |
| 🟠 **P0-B** | Boss 多阶段 + 可读 telegraph | 中 | 高 | Boss 不是血包 |
| 🟡 **P1** | 姐姐情绪 + 道具合成 | 中 | 高 | v1 内容层,在 P0-A 之后 |
| 🟡 **P1** | 隐藏层 + 小剧场 + 独白 | 中 | 中 | 情感层 |
| 🟢 **P2** | 场景互动 + 图鉴 + 难度滑块 | 大 | 中 | 深度内容 |
| ⚪ **P3** | 每日挑战 + 动态 AI + 双人 | 大 | 中 | 长期运营 |

### 1.3 与原文档的关系

- **不修改**已确定的视觉风格、关卡主题、姐姐基础 AI、道具基础数据。
- **扩展**:`src/game/` 下的模块、`assets/manifest` 字段、HUD 显示。
- **新增**:`src/game/emotion/`,`src/game/crafting/`,`src/game/cinematics/`,`src/game/secret/`,`src/game/codex/`,`src/game/save/`,`src/game/dialog/` 等模块。

---

## 2. 设计原则

### 2.1 升级设计要遵守的 5 条原则（v2 修订）

1. **手感优先于内容**——操作、反馈、难度曲线必须先达标,再叠剧情/图鉴/合成。MVP 手感是"刚刚能用",必须**主动改造**,不是"保持不变"。
2. **难度要叠加,不是只调参**——单关内威胁应随进度/时间/表现层层加码(见 §9.4),避免"第 3 关和第 8 关只是数字更大"。
3. **画面要统一,不是贴更多图**——建立 tile 规范、角色描边、视差层、UI 风格;禁止长期用 `makeRect` 色块当正式美术。
4. **每 15 秒一个刺激**——遵循原规划 §8,但 MVP 未落实;升级必须落地为:连击、波次、道具抉择、隐藏入口之一。
5. **可关可调**——所有新系统支持 `FeatureFlags`,但 P0-A 默认全开,不可默认关闭。

### 2.2 不做的设计

- ❌ 不做"真实伤害"或"血腥"表现。保持可爱玩具感。
- ❌ 不做"内购"或"抽卡"机制,本游戏保持纯单机体验。
- ❌ 不做"必须联网"功能。所有数据本地存。
- ❌ 不做"复杂合成树"。道具合成最多 2 选 1,3 选 1 已经是上限。
- ❌ 不做"长剧情过场"。小剧场控制在 10 秒以内,不要电影化。

---

## 3. 互动性升级

互动性升级是本次最核心的部分,目的是让玩家感觉"我在和姐姐打架,而不是在打木桩"。

### 3.1 姐姐情绪系统

#### 3.1.1 设计目标

让姐姐敌人从"靶子"变成"会反应的角色"。通过累积伤害触发不同情绪状态,影响 AI 行为和对话气泡。

#### 3.1.2 状态机

```
[IDLE 平静]
  ↓ 玩家进入视野
[AWARE 警觉]
  ↓ 玩家攻击未命中
[PATROL 巡逻]
  ↓ 玩家攻击命中 1 次
[TSUNDERE 傲娇]    ← 默认打完第一下的反应
  ↓ 玩家攻击命中 2 次
[ANNOYED 烦躁]
  ↓ 玩家攻击命中 3 次
[WRATH 暴怒]      ← 速度+30%,招式+1
  ↓ 玩家远离 5 秒
[CALM_DOWN 平静]  ← 回到 AWARE
  ↓ 玩家攻击命中 4 次
[CRY 委屈]        ← 攻击停止 1.5 秒,头顶冒眼泪
  ↓ 玩家继续攻击
[DEFEAT 失败]
```

#### 3.1.3 情绪数据

```typescript
// src/game/emotion/SisterEmotion.ts
export type EmotionState =
  | "idle"
  | "aware"
  | "patrol"
  | "tsundere"
  | "annoyed"
  | "wrath"
  | "cry"
  | "defeat";

export interface EmotionConfig {
  state: EmotionState;
  hitThreshold: number;      // 进入此状态需要的命中次数
  speedMultiplier: number;    // 移动速度倍率
  attackCooldownMul: number;  // 攻击冷却倍率
  bubbleText: string | null;  // 对话气泡(可选多行)
  bubbleDuration: number;     // 气泡持续秒数
  color: string;              // 状态色(用于头部高光/光圈)
  sfx: string;                // 触发音效 key
}

export const EMOTION_TABLE: Record<EmotionState, EmotionConfig> = {
  idle: {
    state: "idle",
    hitThreshold: 0,
    speedMultiplier: 0.6,
    attackCooldownMul: 1.0,
    bubbleText: null,
    bubbleDuration: 0,
    color: "#ffffff",
    sfx: null,
  },
  aware: {
    state: "aware",
    hitThreshold: 0,
    speedMultiplier: 0.8,
    attackCooldownMul: 1.0,
    bubbleText: "?",
    bubbleDuration: 0.8,
    color: "#ffeb99",
    sfx: "sfx_sister_aware",
  },
  patrol: {
    state: "patrol",
    hitThreshold: 0,
    speedMultiplier: 1.0,
    attackCooldownMul: 1.0,
    bubbleText: null,
    bubbleDuration: 0,
    color: "#ffffff",
    sfx: null,
  },
  tsundere: {
    state: "tsundere",
    hitThreshold: 1,
    speedMultiplier: 1.0,
    attackCooldownMul: 0.9,
    bubbleText: "哼!才不疼!",
    bubbleDuration: 1.5,
    color: "#ffb3c1",
    sfx: "voice_sister_hmph",
  },
  annoyed: {
    state: "annoyed",
    hitThreshold: 2,
    speedMultiplier: 1.15,
    attackCooldownMul: 0.8,
    bubbleText: "你给我等着!",
    bubbleDuration: 1.5,
    color: "#ff9a8b",
    sfx: "voice_sister_wait",
  },
  wrath: {
    state: "wrath",
    hitThreshold: 3,
    speedMultiplier: 1.3,
    attackCooldownMul: 0.7,
    bubbleText: "我要告诉妈妈!",
    bubbleDuration: 2.0,
    color: "#ff5e5e",
    sfx: "voice_sister_mama",
  },
  cry: {
    state: "cry",
    hitThreshold: 4,
    speedMultiplier: 0,
    attackCooldownMul: 999,  // 停止攻击
    bubbleText: "呜呜呜…",
    bubbleDuration: 1.5,
    color: "#9ec5ff",
    sfx: "voice_sister_cry",
  },
  defeat: {
    state: "defeat",
    hitThreshold: 999,
    speedMultiplier: 0,
    attackCooldownMul: 999,
    bubbleText: "我…没输!",
    bubbleDuration: 2.0,
    color: "#cccccc",
    sfx: "sfx_sister_defeat",
  },
};
```

#### 3.1.4 对话气泡池

```typescript
// src/game/emotion/dialogPool.ts
export const SISTER_BUBBLES = {
  // 进入战斗
  enter: ["弟弟你来啦!", "不许跑!", "今天作业写了吗?"],
  // 攻击前
  preAttack: ["看招!", "接住!", "嘿!"],
  // 被打中
  hit: {
    tsundere: ["哼!", "才不疼!", "小意思~", "这算什么!"],
    annoyed: ["哎呀!", "你!你!你!", "别打了!", "疼…"],
    wrath: ["啊啊啊!", "我真的生气了!", "我数到三!", "我要找妈妈!"],
  },
  // 被打哭
  cry: ["呜呜呜…", "好疼啊…", "我要告诉妈妈…", "再打我就哭给你看!"],
  // 被打败
  defeat: ["我…我认输…", "哼,下次一定赢!", "我不服!", "弟弟最厉害了…(小声)"],
  // 玩家受伤
  playerHurt: ["活该!", "知道错了吧!", "下次还敢不敢!"],
  // 看到玩家吃道具
  seeItem: ["那个是我的!", "放下!", "不许吃!"],
  // 看到玩家用火球
  seeFireball: ["啊!烫!", "好热!", "讨厌!"],
};
```

#### 3.1.5 视觉表现

- 头顶气泡:`SisterBubble` 组件,白底 + 黑边 + 圆角,字号 16-20px,显示 1.5-2 秒。
- 状态色:姐姐头部上方有半透明光圈,颜色随状态变(白色 → 粉色 → 红色)。
- 被打哭时:头部冒出小水滴贴图,1.5 秒内逐渐下落。
- 暴怒状态:脚下尘土飞扬,身体轻微红色描边。

#### 3.1.6 实现步骤

1. 创建 `src/game/emotion/SisterEmotion.ts`(状态机 + 配置)。
2. 创建 `src/game/emotion/dialogPool.ts`(对话池)。
3. 创建 `src/game/emotion/SisterBubble.ts`(Phaser 文字气泡组件)。
4. 改造 `EnemyManager`,接入情绪状态。
5. 添加情绪切换的音效 trigger 到 `AudioManager`。
6. 在 `GameScene` 中处理气泡显示和淡出。

#### 3.1.7 资源需求

- **新音频**(4 个):
  - `sfx_sister_aware`(警觉,可用现有人声)
  - `voice_sister_hmph` / `_wait` / `_mama` / `_cry` / `_defeat`(5 个台词)
- **新图像**:头部光圈 spritesheet(8 状态 × 1 帧 = 8 张 64x64 PNG),可选。
- **不需要新角色立绘**:沿用现有 `sister_small` / `sister_headphone` 等。

---

### 3.2 场景可互动对象

#### 3.2.1 设计目标

把当前"只是平台"的场景变成"可以玩"的世界。日常生活物品天生就该能互动。

#### 3.2.2 互动对象清单

| 类别 | 对象 | 互动方式 | 反馈 |
|---|---|---|---|
| 光源 | 台灯 / 吊灯 / 手电 | 按交互键开关 | 关灯 3 秒,敌人视野缩小 |
| 水流 | 水龙头 / 喷头 / 消防栓 | 按交互键打开 | 推动弟弟 + 姐姐,可能冲出宝箱 |
| 家具 | 沙发垫 | 站上去会下沉 | 站得越久弹得越高 |
| 家具 | 抽屉 | 自动周期性伸出/缩回 | 用作移动平台 |
| 家具 | 衣柜门 | 周期性打开 | 吹出强风,推开玩家 |
| 门 | 卧室门 / 浴室门 | 推动 | 进入隐藏区 |
| 装饰 | 相框 / 海报 | 撞击 | 隐藏金币 / 道具 |
| 电器 | 电视机 | 靠近 | 屏幕显示姐弟合影,2 秒内无敌 |
| 玩具 | 玩具箱 | 撞击 | 随机掉 1-3 个道具 |
| 食物 | 冰箱 | 按交互键打开 | 选 1 个道具(吃 / 不用) |

#### 3.2.3 数据结构

```typescript
// src/game/world/Interactive.ts
export type InteractiveType =
  | "light"      // 光源,开关型
  | "water"      // 水流,推力型
  | "spring"     // 弹跳,蓄力型
  | "platform"   // 移动平台
  | "wind"       // 风,推动型
  | "door"       // 门,传送型
  | "decor"      // 装饰,破坏型
  | "tv"         // 电视,buff 型
  | "toybox"     // 玩具箱,产出型
  | "fridge";    // 冰箱,选道具型

export interface InteractiveConfig {
  id: string;
  type: InteractiveType;
  sprite: string;              // 资源 key
  position: { x: number; y: number };
  trigger: "on_touch" | "on_jump" | "on_interact_key";
  cooldown?: number;           // 再次触发的冷却秒数
  payload?: any;               // 各类互动的具体配置
  sfx: string;
  message?: string;            // 屏幕提示文字
}

export const FRIDGE_PAYLOAD = {
  choices: ["fireball_candy", "toy_hammer", "bubble_shield"],
  consumeOnUse: false,         // 选完不消耗
};
```

#### 3.2.4 暗灯机制(高级)

```typescript
// src/game/world/LightSystem.ts
export class LightSystem {
  private darkMode = false;
  private darkTimer = 0;

  enterDark(durationMs: number) {
    this.darkMode = true;
    this.darkTimer = durationMs;
    // 调整全场 alpha
    this.scene.cameras.main.setBackgroundColor("#0a0a1a");
    this.scene.children.list.forEach((obj) => {
      if (obj instanceof Phaser.GameObjects.Sprite) {
        // 在玩家和光源附近的对象保持高亮,其余压暗
        const dist = Phaser.Math.Distance.Between(
          obj.x, obj.y,
          this.player.x, this.player.y
        );
        obj.setAlpha(dist < 200 ? 1 : 0.25);
      }
    });
  }

  update(delta: number) {
    if (!this.darkMode) return;
    this.darkTimer -= delta;
    if (this.darkTimer <= 0) {
      this.darkMode = false;
      this.scene.cameras.main.setBackgroundColor("#87ceeb");
      this.scene.children.list.forEach((obj) => {
        if (obj instanceof Phaser.GameObjects.Sprite) {
          obj.setAlpha(1);
        }
      });
    }
  }
}
```

#### 3.2.5 实现步骤

1. 创建 `src/game/world/InteractiveObject.ts` 通用基类。
2. 在 `levelData.ts` 中为每关配置 2-3 个互动对象。
3. 在 `GameScene` 中检测玩家 + 互动键(E / 上方向键)。
4. 实现 `LightSystem` / `WaterSystem` 等子系统。
5. 给互动对象加 HUD 提示图标(类似 RPG 游戏)。

#### 3.2.6 资源需求

- 互动对象用现有家具素材(沙发、柜子、台灯、水龙头)即可。
- 暗灯特效需要一个新的 spritesheet(4-8 帧光晕)。
- 提示图标可复用现有 128x128 图标(道具图标旋转 45°)。

---

### 3.3 道具合成系统

#### 3.3.1 设计目标

让"捡道具"从"替换"变成"组合",增加道具池的策略深度,让玩家在每次捡道具时多一步决策。

#### 3.3.2 合成规则

| 道具 A | 道具 B | 合成结果 | 效果 |
|---|---|---|---|
| 火球糖 | 爆米花炸弹 | 燃烧爆米花 | 范围燃烧,5 秒地面着火 |
| 泡泡盾 | 飞行帽 | 飞行泡泡 | 飞行时自带护盾 |
| 冰淇淋枪 | 变大饼干 | 巨型冰砖 | 把姐姐冻成大冰块,3 秒 |
| 跳跳鞋 | 星星披风 | 星跳冲刺 | 大跳 + 无敌,可飞跃半个屏幕 |
| 回旋玩具 | 玩具锤 | 回旋飞锤 | 锤子飞出绕身体一圈 |
| 火球糖 | 玩具锤 | 火焰锤 | 锤子带火,落地一片火 |
| 泡泡盾 | 变大饼干 | 巨型泡泡 | 巨大护盾,3 次伤害 |
| 飞行帽 | 星星披风 | 流星冲刺 | 长距离无敌冲刺,穿墙 |
| 冰淇淋枪 | 飞行帽 | 冰雹轰炸 | 飞行时丢冰弹 |
| 爆米花炸弹 | 回旋玩具 | 弹跳炸弹 | 炸弹弹跳 3 次再炸 |

**MVP 阶段先做 5 个最经典的组合**:
1. 火球糖 + 爆米花炸弹 = 燃烧爆米花
2. 泡泡盾 + 飞行帽 = 飞行泡泡
3. 跳跳鞋 + 星星披风 = 星跳冲刺
4. 回旋玩具 + 玩具锤 = 回旋飞锤
5. 冰淇淋枪 + 变大饼干 = 巨型冰砖

#### 3.3.3 触发流程

```
[弟弟已持有 A,捡到 B]
  ↓
[暂停游戏 0.5 秒,屏幕中央弹出合成提示]
  ↓
[3 秒倒计时,显示"按 J 合成"]
  ↓
[按 J:进入合成,生成 A+B,丢掉 A 和 B]
  ↓
[按其他键 / 超时:丢掉 B,保留 A(原行为)]
```

#### 3.3.4 合成提示 UI

```
┌────────────────────────────┐
│  [A图标] + [B图标]  = [AB]  │
│                              │
│      按 J 合成!             │
│      (2.5 秒)               │
│                              │
│      不按就只保留 A         │
└────────────────────────────┘
```

#### 3.3.5 数据结构

```typescript
// src/game/crafting/CraftingRecipe.ts
export interface CraftingRecipe {
  id: string;                       // "fire_candy + popcorn_bomb"
  ingredients: [string, string];    // 道具 id 列表
  result: {
    id: string;                     // "burning_popcorn"
    name: string;                   // "燃烧爆米花"
    type: "active_attack" | "shield" | "movement" | "buff";
    uses?: number;
    durationSeconds?: number;
    action: string;                 // Phaser action key
    description: string;            // "5 秒内地面持续燃烧"
    icon: string;                   // 资源 key
  };
  visual: {
    effect: string;                 // "burn_ground"
    duration: number;               // 持续秒数
  };
  sfx: string;                      // 合成音效
}

export const RECIPES: CraftingRecipe[] = [
  {
    id: "burning_popcorn",
    ingredients: ["fireball_candy", "popcorn_bomb"],
    result: {
      id: "burning_popcorn",
      name: "燃烧爆米花",
      type: "active_attack",
      uses: 3,
      action: "throw_burning_popcorn",
      description: "投出后地面持续燃烧 5 秒",
      icon: "burning_popcorn_hud",
    },
    visual: { effect: "burn_ground", duration: 5 },
    sfx: "sfx_crafting_fire",
  },
  // ... 其他 4 个
];
```

#### 3.3.6 视觉效果

- 合成瞬间:两个道具图同时向屏幕中心"飞"过去。
- 合并瞬间:0.3 秒白屏闪光 + 粒子特效(对应道具色)。
- 落地:新道具从闪光中"跳"出来,带 0.5 秒缩放反弹动画。

#### 3.3.7 实现步骤

1. 创建 `src/game/crafting/CraftingRecipe.ts`(数据)。
2. 创建 `src/game/crafting/CraftingManager.ts`(逻辑)。
3. 创建 `src/game/crafting/CraftingPrompt.ts`(Phaser UI)。
4. 改造 `ItemManager`,在拾取新道具时检测是否能合成。
5. 给 5 个合成道具在 `items-manifest.json` 中加 `craftedFrom` 字段。
6. 实现 5 个新道具的 `action` 函数(放到 `PlayerController`)。

#### 3.3.8 资源需求

- **5 个新道具图标**(512 + 128 两种尺寸)。
- 1 个合成提示 UI 框架(用 Phaser Graphics 画,不需要美术)。
- 1 个合成粒子特效(可用粒子库或简单圆形 + alpha)。
- 1 个新合成音效。

---

### 3.4 弟弟的"反击"机制

#### 3.4.1 设计目标

让弟弟也有"主动挑衅"的动作,强化"反击"主题,而不是被动挨打。

#### 3.4.2 反击动作

| 动作 | 触发 | 效果 |
|---|---|---|
| 挑衅 | 按 T 键 | 弟弟做鬼脸,2 秒内姐姐移动速度 +20%(激怒她) |
| 大叫 | 按 Y 键 | 全屏震波,推开 100px 内所有姐姐 + Boss 取消当前技能 |
| 求饶 | 血量 1 颗心时自动 | 姐姐追击速度 -20%,持续 5 秒,弟弟缓慢回 1 血 |
| 反击跳 | 空中按 J + 下方向 | 下砸时带震波,伤害翻倍 |

#### 3.4.3 "大叫"技能

- 5 秒冷却。
- 屏幕闪白 0.3 秒。
- 周围 100px 内的所有姐姐被推开 200px,有 1 秒硬直。
- Boss 被打断当前技能,进入 0.5 秒眩晕。
- 音效:弟弟大喊"哇啊啊!"+ 声波扩散特效。

#### 3.4.4 数据结构

```typescript
// src/game/player/PlayerAbilities.ts
export interface PlayerAbility {
  id: string;
  name: string;
  key: string;                  // 按键
  cooldownMs: number;
  durationMs?: number;
  effect: (player, scene) => void;
  sfx: string;
}

export const PLAYER_ABILITIES: PlayerAbility[] = [
  {
    id: "taunt",
    name: "挑衅",
    key: "T",
    cooldownMs: 10000,
    durationMs: 2000,
    effect: (player, scene) => {
      // 范围 300px 内姐姐 speedMul = 1.2
      scene.enemyManager.getAllEnemiesInRange(player, 300).forEach(e => {
        e.addBuff("taunted", 2000, { speedMul: 1.2 });
      });
    },
    sfx: "voice_brother_taunt",
  },
  {
    id: "yell",
    name: "大叫",
    key: "Y",
    cooldownMs: 5000,
    effect: (player, scene) => {
      // 屏幕白闪 + 推开 100px 内姐姐
      scene.cameras.main.flash(300, 255, 255, 255);
      scene.enemyManager.getAllEnemiesInRange(player, 100).forEach(e => {
        e.knockback(player.facing, 200);
        e.stun(1000);
      });
      scene.bossManager?.getBoss()?.stun(500);
    },
    sfx: "voice_brother_yell",
  },
  // ...
];
```

#### 3.4.5 实现步骤

1. 创建 `src/game/player/PlayerAbilities.ts`。
2. 在 `PlayerController` 中加入新按键监听(T / Y)。
3. 在 HUD 显示技能冷却条(底部一排小图标 + 倒计时数字)。
4. 加入屏幕震屏 + 白屏 flash 调用。
5. 给姐姐加 `addBuff` / `stun` / `knockback` 方法。

#### 3.4.6 资源需求

- 2 个弟弟语音(挑衅 + 大叫)。
- 1 个挑衅表情 spritesheet(2-3 帧)。
- 1 个震波粒子(可用现有星星贴图)。
- 不需要新角色立绘。

---

## 4. 可玩性升级

### 4.1 隐藏层与支线房间

#### 4.1.1 设计目标

让每个关卡从"走过去"变成"探索",大幅提升重玩价值。

#### 4.1.2 隐藏层类型

| 类型 | 入口 | 内容 | 奖励 |
|---|---|---|---|
| 回忆房间 | 用星星披风穿透的墙 | 3 段无声小动画(2 秒 × 3) | 100 金币 + 1 个徽章 |
| 挑战房间 | 蹲下进入的洞 | 30 秒限时打怪 | 专属皮肤(姐姐) |
| 彩蛋房间 | 飞行帽飞到的高台 | "小号姐姐"对话,获得信息 | 解锁姐姐日记 |
| 奖励房间 | 管道 | 大量金币 + 1 个随机道具 | 500 金币 |
| 商店房间 | 隐藏门 | 用金币买皮肤 / 道具 | 永久解锁道具 |
| 镜像房间 | 镜子 | 反向关卡(从右往左走) | 双倍金币 |

#### 4.1.3 每关分布

- 每关至少 1 个隐藏入口(挑战 / 回忆 二选一)。
- 每 3 关 1 个彩蛋房间(累计 4-5 个彩蛋覆盖主线剧情)。
- 商店房间在关卡 1-2, 4-5, 7-8 中各 1 个。
- 镜像房间作为"奖励关",每章最后一关有 1 个。

#### 4.1.4 数据结构

```typescript
// src/game/secret/SecretLevel.ts
export type SecretType =
  | "memory"   // 回忆
  | "challenge" // 限时挑战
  | "easter_egg" // 彩蛋
  | "bonus"    // 奖励
  | "shop"     // 商店
  | "mirror";  // 镜像

export interface SecretLevelConfig {
  id: string;
  parentLevel: string;          // 所属关卡
  type: SecretType;
  entry: {
    position: { x: number; y: number };
    condition: "star_cape" | "fly_cap" | "duck" | "pipe" | "key_item" | "hidden_door";
    hint?: string;              // 屏幕角落的灰色提示文字(可选)
  };
  length: number;               // 隐藏关卡长度(像素)
  rewards: {
    coins: number;
    badge?: string;
    skin?: string;
    story?: string;             // 故事 id
  };
  dialogue?: DialogueNode[];    // 对话节点(彩蛋 / 商店用)
}

export const SECRET_LEVELS: SecretLevelConfig[] = [
  {
    id: "secret_1_1_memory",
    parentLevel: "1_1",
    type: "memory",
    entry: {
      position: { x: 2400, y: 320 },
      condition: "star_cape",
      hint: "墙上好像有裂缝…",
    },
    length: 800,
    rewards: { coins: 100, badge: "memory_school" },
  },
  // ...
];
```

#### 4.1.5 入口提示系统

```typescript
// src/game/secret/SecretHint.ts
export class SecretHint {
  private static shownHints = new Set<string>();

  static maybeShowHint(player, scene) {
    const dist = 200;  // 玩家离入口 200px 内
    SECRET_LEVELS.forEach(secret => {
      const d = Phaser.Math.Distance.Between(
        player.x, player.y,
        secret.entry.position.x, secret.entry.position.y
      );
      if (d < dist && !this.shownHints.has(secret.id)) {
        this.shownHints.add(secret.id);
        // 屏幕底部灰色小字提示
        scene.showToast(secret.entry.hint ?? "发现隐藏入口!", "#888888");
      }
    });
  }
}
```

#### 4.1.6 实现步骤

1. 创建 `src/game/secret/SecretLevel.ts` 数据。
2. 创建 `src/game/secret/SecretScene.ts`(通用隐藏关卡场景)。
3. 创建 `src/game/secret/SecretHint.ts`(提示系统)。
4. 在每个关卡 `levelData.ts` 中加 `secrets: SecretLevelConfig[]` 字段。
5. 实现 6 种入口检测(star_cape / fly_cap / duck / pipe / key_item / hidden_door)。
6. 实现隐藏关卡的结算(奖励结算 + 返回主关卡)。

#### 4.1.7 资源需求

- 回忆房间需要 18 段(每关 3 段 × 6 主题)无声小插画。
- 商店需要 6 个商品图标(可复用现有道具图标)。
- 镜像关直接复用关卡数据,不需要新资源。

---

### 4.2 动态难度与"姐姐懂你"

#### 4.2.1 设计目标

让姐姐敌人根据玩家历史行为调整策略,产生"姐姐在针对我"的感觉,大幅提升代入感。

#### 4.2.2 行为追踪

```typescript
// src/game/ai/PlayerBehavior.ts
export interface PlayerBehaviorStats {
  jumpsPerMinute: number;        // 跳得多
  usesFireball: number;          // 爱用火球
  usesHammer: number;            // 爱用锤子
  usesShield: number;            // 爱用盾
  backtracksOften: boolean;      // 经常回头
  rushesForward: boolean;        // 一路往前冲
  diesOnSpikes: number;          // 经常被尖刺戳死
  diesOnFalling: number;         // 经常掉坑
  diesOnBoss: number;            // 经常被 Boss 打死
  averageLevelTime: number;      // 平均关卡时间
  recentItemIds: string[];       // 最近用过的道具
}

export const EMPTY_BEHAVIOR: PlayerBehaviorStats = {
  jumpsPerMinute: 0,
  usesFireball: 0,
  usesHammer: 0,
  usesShield: 0,
  backtracksOften: false,
  rushesForward: false,
  diesOnSpikes: 0,
  diesOnFalling: 0,
  diesOnBoss: 0,
  averageLevelTime: 0,
  recentItemIds: [],
};
```

#### 4.2.3 反制策略

| 玩家特征 | 姐姐反制 |
|---|---|
| 跳得多 | 姐姐小怪改为**蹲守头顶**,耳机姐姐音波改为**对空发射** |
| 爱用火球 | Boss 召唤**泡泡盾**反制,小怪减少出现 |
| 爱用锤子 | 增加**远程敌人**(管道姐姐)数量,减少近战敌人 |
| 爱用盾 | 敌人加"穿透攻击",Boss 二阶段直接没收盾 |
| 经常回头 | 隐藏门反向,真正的彩蛋在前 1/3 而不是后 1/3 |
| 一路往前冲 | 加**滚石追击**和**限时路段** |
| 经常被尖刺戳 | 尖刺改为**更明显的红色** + 提前 0.5 秒预警 |
| 经常掉坑 | 关卡加**隐形护栏**(只对掉坑多次的玩家显示) |
| 经常被 Boss 打死 | Boss 攻击速度 -10% |

#### 4.2.4 实现方式

- 所有数据用 `localStorage` 存,key = `brother_behavior_v1`。
- 每次关卡结束 / 玩家死亡时更新数据。
- 关卡开始时读取数据,应用反制策略到本关配置中。
- 玩家可以重置数据(设置里"恢复默认难度")。

#### 4.2.5 数据结构

```typescript
// src/game/ai/BehaviorTracker.ts
export class BehaviorTracker {
  static STORAGE_KEY = "brother_behavior_v1";

  static load(): PlayerBehaviorStats {
    const raw = localStorage.getItem(this.STORAGE_KEY);
    if (!raw) return { ...EMPTY_BEHAVIOR };
    try {
      return { ...EMPTY_BEHAVIOR, ...JSON.parse(raw) };
    } catch {
      return { ...EMPTY_BEHAVIOR };
    }
  }

  static save(stats: PlayerBehaviorStats) {
    localStorage.setItem(this.STORAGE_KEY, JSON.stringify(stats));
  }

  static update(event: PlayerEvent) {
    const stats = this.load();
    switch (event.type) {
      case "jump": stats.jumpsPerMinute++; break;
      case "use_item":
        if (event.itemId === "fireball_candy") stats.usesFireball++;
        // ...
        break;
      case "die":
        if (event.cause === "spike") stats.diesOnSpikes++;
        // ...
        break;
    }
    this.save(stats);
  }

  static applyCounterMeasures(level: LevelConfig, stats: PlayerBehaviorStats): LevelConfig {
    const modified = { ...level };
    if (stats.usesFireball > 10) {
      modified.bosses?.forEach(b => b.hasShield = true);
    }
    if (stats.diesOnFalling > 3) {
      modified.invisibleRails = true;  // 掉坑 3 次以上的玩家显示护栏
    }
    return modified;
  }
}
```

#### 4.2.6 隐私说明

- 数据全部本地存,不上传。
- 设置页提供"清除我的游戏数据"按钮。
- 第一次进入游戏时显示简单说明:"游戏会根据你的玩法调整难度"。

#### 4.2.7 实现步骤

1. 创建 `src/game/ai/BehaviorTracker.ts`。
2. 在 `PlayerController` / `ItemManager` / `GameScene` 关键事件点调用 `update`。
3. 在 `LevelManager.loadLevel` 中调用 `applyCounterMeasures`。
4. 在设置页加"清除数据"按钮。
5. 在 README 加隐私说明。

#### 4.2.8 资源需求

- 几乎不需要新资源,纯逻辑改动。
- 唯一新增:姐姐小怪"蹲守头顶"的新 spritesheet(2-3 帧)。

---

### 4.3 每日 / 每周挑战

#### 4.3.1 设计目标

让游戏有"每天都来看看"的理由,而不是"通关就不玩"。

#### 4.3.2 每日挑战规则

- 每天 0:00 重置。
- 挑战内容 = 1 个固定种子关 + 固定道具池 + 固定难度修饰。
- 玩家只能用 1 颗心通关。
- 通关奖励 = 1 张"姐姐日记"卡片。

#### 4.3.3 每周挑战规则

- 每周一 0:00 重置。
- 挑战内容 = 5 个迷你关串联(每关 30 秒)。
- 限制条件(每轮换):"只用泡泡盾"、"不能跳"、"不许踩怪"、"只能用火球"、"反向通关"。
- 通关奖励 = 1 个稀有皮肤(随机)。

#### 4.3.4 种子系统

```typescript
// src/game/daily/DailySeed.ts
export function getDailySeed(): string {
  const today = new Date();
  return `${today.getFullYear()}-${today.getMonth() + 1}-${today.getDate()}`;
}

export function seededRandom(seed: string): () => number {
  // 简单的字符串 hash + LCG
  let h = 0;
  for (let i = 0; i < seed.length; i++) {
    h = (h << 5) - h + seed.charCodeAt(i);
    h |= 0;
  }
  return () => {
    h = (h * 9301 + 49297) % 233280;
    return h / 233280;
  };
}

export function generateDailyLevelConfig(seed: string): LevelConfig {
  const rand = seededRandom(seed);
  return {
    id: `daily_${seed}`,
    name: `每日挑战 · ${seed}`,
    maxHp: 1,
    music: "bgm_fight_loop",
    enemies: pickEnemiesBySeed(rand),
    items: pickItemsBySeed(rand, ["fireball_candy", "bubble_shield"]),  // 限制道具池
    platforms: generatePlatformsBySeed(rand, 2000, 720),
    // ...
  };
}
```

#### 4.3.5 数据结构

```typescript
// src/game/daily/DailyChallenge.ts
export interface DailyChallenge {
  seed: string;
  date: string;
  config: LevelConfig;
  modifiers: {
    hp: number;
    itemPool: string[];
    bossHp?: number;
    enemySpeedMul?: number;
  };
  reward: {
    type: "diary_card" | "skin" | "badge";
    id: string;
  };
  leaderboard: DailyEntry[];     // 本地前 3 名
}

export interface WeeklyChallenge {
  weekId: string;                // "2026-W25"
  levels: LevelConfig[];         // 5 个迷你关
  restriction: string;           // "只用泡泡盾" 等
  reward: {
    type: "rare_skin";
    pool: string[];
  };
  completed: boolean;
}
```

#### 4.3.6 存档

```typescript
// src/game/daily/DailyStore.ts
export const DailyStore = {
  getLastDaily(): DailyChallenge | null {
    const raw = localStorage.getItem(`daily_${getDailySeed()}`);
    return raw ? JSON.parse(raw) : null;
  },
  saveCompletion(time: number, deaths: number) {
    const seed = getDailySeed();
    const key = `daily_${seed}`;
    const current = this.getLastDaily() ?? { ... };
    current.leaderboard.push({ time, deaths, date: new Date().toISOString() });
    current.leaderboard.sort((a, b) => a.time - b.time);
    current.leaderboard = current.leaderboard.slice(0, 10);
    localStorage.setItem(key, JSON.stringify(current));
  },
};
```

#### 4.3.7 实现步骤

1. 创建 `src/game/daily/DailySeed.ts` 种子系统。
2. 创建 `src/game/daily/DailyChallenge.ts` 数据。
3. 创建 `src/game/daily/DailyStore.ts` 存档。
4. 创建 `src/game/daily/DailyScene.ts` 每日挑战场景。
5. 在主菜单加"今日挑战"入口,显示今日种子 + 今日限制。
6. 在主菜单加"本周挑战"入口。
7. 结算时奖励"姐姐日记"卡片(可查看)。

#### 4.3.8 资源需求

- 不需要新关卡美术,程序生成。
- 每日挑战结算 UI 用现有 UI 组件。
- 姐姐日记需要 7 张卡(一周)或 30 张卡(一个月),每张是固定插画 + 一段文字。

---

### 4.4 难度滑块

#### 4.4.1 设计目标

让玩家自己控制难度,比"简单/挑战"二元选择更细腻。

#### 4.4.2 滑块设置

```
弟弟生命:    [1 ●━━━━━━━━ 5]   (滑块 1-5,默认 3)
姐姐速度:    [━━━●━━━━━━] 100%  (50%-150%,默认 100%)
道具频率:    [━━━━━●━━━━] 100%  (0%-200%,默认 100%)
陷阱伤害:    [━━━━●━━━━━] 1     (0-3,默认 1)
无敌时间:    [━━━━●━━━━━] 1.5s  (0.5-3.0s,默认 1.5s)
```

#### 4.4.3 预设

- **轻松模式**:生命=5, 速度=70%, 频率=150%, 伤害=0
- **标准模式**(默认):生命=3, 速度=100%, 频率=100%, 伤害=1
- **挑战模式**:生命=1, 速度=130%, 频率=50%, 伤害=1
- **噩梦模式**:生命=1, 速度=150%, 频率=0%, 伤害=3, 无敌=0.5s

#### 4.4.4 数据结构

```typescript
// src/game/config/DifficultyConfig.ts
export interface DifficultyConfig {
  playerMaxHp: number;          // 1-5
  enemySpeedMul: number;        // 0.5-1.5
  itemDropMul: number;          // 0-2
  trapDamage: number;           // 0-3
  invincibilityMs: number;      // 500-3000
  bossHpMul: number;            // 0.5-2
  fallDamageMul: number;        // 0-2
}

export const DEFAULT_DIFFICULTY: DifficultyConfig = {
  playerMaxHp: 3,
  enemySpeedMul: 1.0,
  itemDropMul: 1.0,
  trapDamage: 1,
  invincibilityMs: 1500,
  bossHpMul: 1.0,
  fallDamageMul: 1.0,
};

export const PRESETS: Record<string, DifficultyConfig> = {
  easy: { ...DEFAULT_DIFFICULTY, playerMaxHp: 5, enemySpeedMul: 0.7, itemDropMul: 1.5, trapDamage: 0 },
  standard: DEFAULT_DIFFICULTY,
  challenge: { ...DEFAULT_DIFFICULTY, playerMaxHp: 1, enemySpeedMul: 1.3, itemDropMul: 0.5 },
  nightmare: { ...DEFAULT_DIFFICULTY, playerMaxHp: 1, enemySpeedMul: 1.5, itemDropMul: 0, trapDamage: 3, invincibilityMs: 500, bossHpMul: 2 },
};
```

#### 4.4.5 UI 草图

```
┌──────────────────────────────┐
│   难度调整                    │
│                              │
│   弟弟生命                   │
│   [滑块 1-5]    3            │
│                              │
│   姐姐速度                   │
│   [滑块 50%-150%]  100%      │
│                              │
│   道具频率                   │
│   [滑块 0-200%]  100%        │
│                              │
│   陷阱伤害                   │
│   [滑块 0-3]    1            │
│                              │
│   ────────────────────       │
│   [轻松] [标准] [挑战] [噩梦] │
│                              │
│       [保存] [取消]          │
└──────────────────────────────┘
```

#### 4.4.6 实现步骤

1. 创建 `src/game/config/DifficultyConfig.ts`。
2. 创建 `src/game/config/DifficultyStore.ts`(localStorage 存取)。
3. 在 `EnemyManager` / `ItemManager` / `PlayerController` 中读取难度配置。
4. 创建 `src/game/ui/DifficultyPanel.ts` UI 组件。
5. 在主菜单和暂停菜单加"难度"入口。

#### 4.4.7 资源需求

- 不需要新资源,纯 UI 改动。
- 滑块可用 Phaser 自带或引入 1 个轻量 UI 库(可选)。

---

### 4.5 收藏图鉴系统

#### 4.5.1 设计目标

让玩家有"收集控"动力,延长游戏生命周期。

#### 4.5.2 图鉴分类

| 分类 | 数量 | 收集条件 | 备注 |
|---|---|---|---|
| 道具图鉴 | 10 普通 + 5 合成 = 15 | 使用过 1 次 | 解锁后可以看到立绘 + 描述 |
| 姐姐图鉴 | 5 基础 + 5 强化 + 4 Boss = 14 | 击败过 1 次 | 解锁后可以看到立绘 + 姐姐日记 |
| 关卡图鉴 | 10 关 | 通关 | 解锁后可以看到关卡概念图 |
| 徽章图鉴 | 30+ | 满足条件 | 进度条显示 |
| 皮肤图鉴 | 8+ | 解锁 | 皮肤预览 |

#### 4.5.3 图鉴数据结构

```typescript
// src/game/codex/CodexEntry.ts
export interface CodexEntry {
  id: string;
  category: "item" | "enemy" | "level" | "badge" | "skin";
  name: string;
  description: string;
  unlockCondition: string;
  art: {
    portrait: string;           // 资源 key
    background?: string;        // 背景 key
  };
  story?: string;               // 姐姐日记 / 弟弟独白(可选)
  stats?: { [key: string]: any };  // 道具 / 敌人的数据
}

export const CODEX_ITEMS: CodexEntry[] = [
  {
    id: "fireball_candy",
    category: "item",
    name: "火球糖",
    description: "可以发射 5 次小火球的糖果,小范围燃烧。",
    unlockCondition: "使用 1 次火球糖",
    art: { portrait: "fireball_candy" },
    stats: { uses: 5, damage: 1, type: "active_attack" },
  },
  // ...
];
```

#### 4.5.4 姐姐日记(每打败 1 个姐姐,解锁 1 篇)

```typescript
// src/game/codex/SisterDiary.ts
export interface DiaryEntry {
  id: string;
  sisterId: string;             // 哪个姐姐
  trigger: "first_defeat" | "level_complete" | "secret_room" | "challenge_complete";
  title: string;
  content: string;              // 200-300 字
  art: string;                  // 配图 key
  unlockHint?: string;          // 灰色提示
}

export const DIARY_ENTRIES: DiaryEntry[] = [
  {
    id: "diary_sister_small_01",
    sisterId: "sister_small",
    trigger: "first_defeat",
    title: "姐姐的小本本",
    content: "今天弟弟又把客厅弄乱了。我跟他说三次了,把玩具收起来。他不听,还在那儿冲我做鬼脸。哎,等他长大一点应该就懂事了。",
    art: "diary_sister_small_01",
  },
  {
    id: "diary_sister_boss_01",
    sisterId: "sister_boss",
    trigger: "first_defeat",
    title: "姐姐的真心话",
    content: "其实我也不想当姐姐。弟弟总说我不陪他玩,但我每天要做作业、上补习班、还要照顾他。我也想有人陪我玩啊…",
    art: "diary_sister_boss_01",
  },
  // ...
];
```

#### 4.5.5 UI 草图

```
┌────────────────────────────────────────┐
│   收藏图鉴                              │
│                                        │
│   [道具] [姐姐] [关卡] [徽章] [皮肤]    │
│                                        │
│   ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│   │ 火球 │ │ 锤子 │ │ 跳鞋 │ │ ？？ │  │
│   │ 糖   │ │      │ │      │ │      │  │
│   │  ✓   │ │  ✓   │ │  ✓   │ │ ??? │  │
│   └──────┘ └──────┘ └──────┘ └──────┘  │
│                                        │
│   ──────────────────────────            │
│   [火球糖]                              │
│   可以发射 5 次小火球的糖果。           │
│   "弟弟的幻想零食武器。"                │
│                                        │
│   收集进度: 5/15                        │
└────────────────────────────────────────┘
```

#### 4.5.6 实现步骤

1. 创建 `src/game/codex/CodexEntry.ts` 数据。
2. 创建 `src/game/codex/CodexStore.ts` 解锁追踪(localStorage)。
3. 创建 `src/game/codex/SisterDiary.ts` 姐姐日记内容。
4. 创建 `src/game/codex/CodexScene.ts` 图鉴场景。
5. 在主菜单加"图鉴"入口。
6. 在游戏中首次使用 / 击败时记录解锁。

#### 4.5.7 资源需求

- 道具 / 敌人立绘用现有资源。
- 姐姐日记配图 14 张(每个姐姐 1 张,Q 版日记本风格)。
- 不需要新 UI 组件,沿用现有 Phaser 组件。

---

## 5. 情感剧情升级

### 5.1 关卡间小剧场

#### 5.1.1 设计目标

让游戏有"故事感",而不只是"打怪"。每章开头和结尾有 5-10 秒的小剧场,揭示姐姐和弟弟的关系。

#### 5.1.2 章节剧场脚本(示例)

**第一章 放学回家路**

开头:
- 画面:夕阳,校门口,弟弟走出。
- 姐姐突然出现:"今天作业写了吗?"
- 弟弟幻想场景淡入,变成游戏关卡。

结尾:
- 画面:弟弟跑回家门口。
- 姐姐站在门口:"站住!你跑哪去了!"
- 弟弟(小声):"…姐,你不是在家吗?"

**第二章 家里大战**

开头:
- 画面:客厅,弟弟在沙发上跳。
- 姐姐拿拖把进来:"鞋子脱了没?"
- 弟弟幻想场景淡入。

结尾:
- 画面:餐桌,弟弟被罚坐。
- 姐姐端菜:"吃饭了。先把作业写完。"

#### 5.1.3 实现方式

- 小剧场是 1-3 张静态插画 + 文字对话气泡。
- 用 Phaser 的 Scene 系统实现,`CinematicScene` 通用类。
- 玩家可按任意键跳过。

```typescript
// src/game/cinematics/CinematicScene.ts
export interface CinematicFrame {
  background: string;            // 资源 key
  characters: {
    id: string;                  // 弟弟 / 姐姐
    position: "left" | "center" | "right";
    pose: string;                // 站 / 跳 / 生气 / 笑
  }[];
  dialogue: {
    speaker: "brother" | "sister" | "narrator";
    text: string;
    duration: number;            // 持续秒数
  }[];
  nextFrame?: string;            // 下一帧 key
}

export interface Cinematic {
  id: string;
  trigger: "level_start" | "level_end" | "chapter_start" | "chapter_end" | "boss_defeat";
  levelId: string;
  frames: CinematicFrame[];
  musicKey?: string;
  skippable: boolean;
}
```

#### 5.1.4 资源需求

- 章节小剧场背景 9 张(每章 2-3 张,合计约 10 张)。
- 姐姐 / 弟弟立绘 4 种姿势(站、跑、笑、生气)。
- 不需要新音效。

---

### 5.2 姐姐的真心话

#### 5.2.1 设计目标

打败姐姐 / Boss 后,显示姐姐的"真心话",让玩家意识到"姐姐其实是关心弟弟的"。

#### 5.2.2 真心话内容(部分示例)

**姐姐小怪 被打败后**:
- "哼,弟弟你等着…"
- "其实我只是想跟你玩…"
- "下次我一定赢!"
- "作业你到底写没写啊!"

**耳机姐姐 被打败后**:
- "我把音乐开大声点是想盖住妈妈的唠叨…"
- "哎,作业太多了…"

**Boss 姐姐 被打败后**:
- "弟弟…其实姐姐也不想追你…"
- "我也想玩,但作业没写完…"
- "谢谢你陪姐姐玩了一局…"

**最终 Boss 姐姐 被打败后**:
- "弟弟,姐姐认输了。…你愿意陪姐姐出去散散步吗?"
- (此时出现剧情过场:姐姐和弟弟一起走出房间)

#### 5.2.3 数据结构

```typescript
// src/game/dialog/HeartfeltLines.ts
export interface HeartfeltLine {
  id: string;
  sisterId: string;             // 哪个姐姐
  trigger: "after_defeat" | "after_boss_defeat" | "after_final_boss";
  lines: string[];              // 随机 1 条
  weight?: number;              // 出现权重
  requiresFlag?: string;        // 需要某个剧情 flag
}

export const HEARTFELT_LINES: HeartfeltLine[] = [
  {
    id: "small_after_1",
    sisterId: "sister_small",
    trigger: "after_defeat",
    lines: [
      "哼,弟弟你等着!",
      "才不疼呢!",
      "下次我一定赢!",
    ],
  },
  {
    id: "boss_after_1",
    sisterId: "sister_boss",
    trigger: "after_boss_defeat",
    lines: [
      "弟弟…其实姐姐也不想追你…",
      "我也想玩,但作业没写完…",
      "谢谢你陪姐姐玩了一局…",
    ],
  },
];
```

#### 5.2.4 UI 表现

- 打败姐姐后,屏幕渐暗(0.5 秒)。
- 姐姐在画面中央,头顶有对话气泡。
- 文字逐字显示(打字机效果),2 秒后渐出。
- 配合柔和的钢琴 BGM(用现有 `theme_sister` 变奏)。

#### 5.2.5 实现步骤

1. 创建 `src/game/dialog/HeartfeltLines.ts` 数据。
2. 创建 `src/game/dialog/DialogBubble.ts` UI 组件。
3. 在 `GameScene` 中敌人血量归零时,触发真心话。
4. 在主菜单加"查看姐姐日记"入口,汇总所有已解锁的真心话。

---

### 5.3 弟弟的内心独白

#### 5.3.1 设计目标

让弟弟在游戏中偶尔"自言自语",增加代入感,让玩家感觉"这就是个有想象力的小男孩"。

#### 5.3.2 触发场景

| 场景 | 独白 |
|---|---|
| 看到冰箱 | "这个…可以变成武器吗?" |
| 看到作业本 | "完了完了…这是啥?" |
| 看到姐姐睡觉 | "嘿嘿,趁现在!" |
| 进入浴室 | "泡泡…好像云一样~" |
| 看到月亮 | "月亮好大,真想爬上去~" |
| 看到玩具 | "姐姐的玩具!偷玩一下!" |
| 跳过失败 | "呜…再来一次!" |
| 捡到火球糖 | "哇!这个我喜欢!" |
| 看到 Boss | "姐…姐姐?!你…你怎么这么大?" |

#### 5.3.3 数据结构

```typescript
// src/game/dialog/BrotherMonologue.ts
export interface MonologueTrigger {
  id: string;
  trigger: "scene_event" | "item_pickup" | "enemy_seen" | "level_event" | "fall_recover" | "boss_enter";
  condition?: {
    minLevel?: number;          // 至少第几关
    flag?: string;              // 需要剧情 flag
    probability?: number;       // 触发概率(0-1)
  };
  text: string;
  bubbleDuration: number;       // 默认 2.5 秒
}

export const BROTHER_MONOLOGUES: MonologueTrigger[] = [
  {
    id: "see_fridge",
    trigger: "scene_event",
    condition: { minLevel: 2, probability: 0.7 },
    text: "这个…可以变成武器吗?",
    bubbleDuration: 2.5,
  },
  {
    id: "see_homework",
    trigger: "scene_event",
    condition: { minLevel: 4, probability: 0.8 },
    text: "完了完了…这是啥?",
    bubbleDuration: 2.0,
  },
  // ...
];
```

#### 5.3.4 UI 表现

- 弟弟头顶小气泡(比姐姐的小一号)。
- 黄色或浅蓝色背景,圆角。
- 打字机效果显示,1.5-2.5 秒后渐出。
- 同一独白在 3 分钟内不重复触发。

#### 5.3.5 实现步骤

1. 创建 `src/game/dialog/BrotherMonologue.ts` 数据。
2. 在 `GameScene` 各事件点(进入新区域、拾取道具、看到 Boss)调用 `tryTrigger`。
3. 复用 `DialogBubble` UI 组件,新增 `speaker: "brother"` 参数。

#### 5.3.6 资源需求

- 不需要新资源,沿用现有气泡 UI。

---

### 5.4 故事线骨架

#### 5.4.1 整体故事线

```
第 1 章 放学回家路
  └─ 弟弟被姐姐一路追,想反击
  └─ 故事点:姐姐在追弟弟时踩到一个玩具滑倒
第 2 章 家里大战
  └─ 弟弟跑回家以为安全了,姐姐在家等着
  └─ 故事点:姐姐其实在给弟弟准备生日蛋糕
第 3 章 学校球场
  └─ 弟弟幻想自己是运动英雄
  └─ 故事点:姐姐在球场当教练,鼓励弟弟
第 4 章 教室大作战
  └─ 弟弟面对"作业"主题的姐姐
  └─ 故事点:姐姐的作业本里夹着弟弟小时候的照片
第 5-8 章 各种主题战斗
  └─ 姐姐的各种形态
第 9 章 最终幻想房间
  └─ 多阶段 Boss 战
  └─ 真结局:姐姐和弟弟一起吃冰淇淋,幻想结束
```

#### 5.4.2 剧情 Flag 系统

```typescript
// src/game/dialog/StoryFlags.ts
export const STORY_FLAGS = {
  // 第 1 章
  chapter1_balloon_defeated: "chapter1_balloon_defeated",
  chapter1_boss_seen_locket: "chapter1_boss_seen_locket",
  // 第 2 章
  chapter2_cake_found: "chapter2_cake_found",
  // ...
};

export class StoryFlagStore {
  static KEY = "brother_story_flags_v1";

  static load(): Set<string> {
    const raw = localStorage.getItem(this.KEY);
    if (!raw) return new Set();
    return new Set(JSON.parse(raw));
  }

  static save(flags: Set<string>) {
    localStorage.setItem(this.KEY, JSON.stringify([...flags]));
  }

  static set(flag: string) {
    const flags = this.load();
    flags.add(flag);
    this.save(flags);
  }

  static has(flag: string): boolean {
    return this.load().has(flag);
  }
}
```

#### 5.4.3 真结局触发条件

```
1. 完成 1-9 章所有主线关卡
2. 收集所有姐姐日记(14 篇)
3. 击败最终 Boss 且未使用任何"大叫"技能
4. STORY_FLAGS 中至少有 5 个 "好感度" 标记
→ 触发真结局:姐姐和弟弟坐在草地上,夕阳,对话。
```

---

## 6. 技术实现

### 6.1 工程结构扩展

```
src/
  main.ts
  style.css
  game/
    assetResolver.ts
    GameScene.ts
    levelData.ts
    MenuScenes.ts
    PreloadScene.ts
    types.ts
    config/                      # 新增
      audioConfig.ts
      DifficultyConfig.ts        # 新增
      DifficultyStore.ts         # 新增
    save/                        # 新增
      SaveManager.ts
      DifficultyStore.ts
      StoryFlagStore.ts
    emotion/                     # 新增
      SisterEmotion.ts
      SisterBubble.ts
      dialogPool.ts
    crafting/                    # 新增
      CraftingRecipe.ts
      CraftingManager.ts
      CraftingPrompt.ts
    player/                      # 新增
      PlayerAbilities.ts
    world/                       # 新增
      InteractiveObject.ts
      LightSystem.ts
      WaterSystem.ts
    secret/                      # 新增
      SecretLevel.ts
      SecretScene.ts
      SecretHint.ts
    ai/                          # 新增
      BehaviorTracker.ts
    daily/                       # 新增
      DailySeed.ts
      DailyChallenge.ts
      DailyScene.ts
      DailyStore.ts
    codex/                       # 新增
      CodexEntry.ts
      CodexStore.ts
      SisterDiary.ts
      CodexScene.ts
    dialog/                      # 新增
      HeartfeltLines.ts
      BrotherMonologue.ts
      DialogBubble.ts
    cinematics/                  # 新增
      CinematicScene.ts
      ChapterPrologue.ts
      ChapterEpilogue.ts
    ui/                          # 新增
      DifficultyPanel.ts
      CinematicTransition.ts
      Toast.ts
```

### 6.2 状态机

#### 6.2.1 姐姐情绪状态机

```typescript
// src/game/emotion/SisterStateMachine.ts
export class SisterStateMachine {
  private state: EmotionState = "idle";
  private hitCount: number = 0;
  private stateTimer: number = 0;

  constructor(
    private owner: Phaser.GameObjects.Sprite,
    private config: { onStateChange?: (s: EmotionState) => void; }
  ) {}

  onHit() {
    this.hitCount++;
    const newState = this.computeState();
    if (newState !== this.state) {
      this.transitionTo(newState);
    }
  }

  onPlayerFar() {
    if (this.state === "wrath" || this.state === "annoyed") {
      this.transitionTo("calm_down");
    }
  }

  update(delta: number) {
    // 状态持续时间逻辑
    // ...
  }

  private computeState(): EmotionState {
    if (this.hitCount >= 4) return "cry";
    if (this.hitCount >= 3) return "wrath";
    if (this.hitCount >= 2) return "annoyed";
    if (this.hitCount >= 1) return "tsundere";
    return "patrol";
  }

  private transitionTo(s: EmotionState) {
    this.state = s;
    const cfg = EMOTION_TABLE[s];
    this.owner.setTint(this.hexToInt(cfg.color));
    this.config.onStateChange?.(s);
    // 触发音效 + 气泡
  }

  private hexToInt(hex: string): number {
    return parseInt(hex.replace("#", ""), 16);
  }
}
```

#### 6.2.2 玩家状态机

```typescript
// src/game/player/PlayerStateMachine.ts
export type PlayerState =
  | "idle"
  | "running"
  | "jumping"
  | "falling"
  | "stomping"        // 下砸
  | "hurting"         // 受伤闪烁
  | "crying"          // 哭哭
  | "attacking"       // 用道具
  | "dashing"         // 星星披风冲刺
  | "flying"          // 飞行帽
  | "dead";

export const PLAYER_STATE_PRIORITY: PlayerState[] = [
  "dead", "crying", "hurting", "stomping", "attacking", "dashing", "flying",
  "jumping", "falling", "running", "idle",
];
```

### 6.3 数据结构

#### 6.3.1 完整类型定义(汇总)

```typescript
// src/game/types.ts (扩展)
export type EmotionState =
  | "idle" | "aware" | "patrol" | "tsundere"
  | "annoyed" | "wrath" | "cry" | "defeat";

export type InteractiveType =
  | "light" | "water" | "spring" | "platform"
  | "wind" | "door" | "decor" | "tv" | "toybox" | "fridge";

export type SecretType =
  | "memory" | "challenge" | "easter_egg"
  | "bonus" | "shop" | "mirror";

export type CodexCategory =
  | "item" | "enemy" | "level" | "badge" | "skin";

export type CinematicTrigger =
  | "level_start" | "level_end" | "chapter_start" | "chapter_end" | "boss_defeat";

export interface GameSave {
  version: string;             // "1.0.0"
  unlockedLevels: string[];
  badges: string[];
  skins: string[];
  coins: number;
  totalDeaths: number;
  bestTimes: Record<string, number>;  // 关卡 id → 最佳时间
  diaryUnlocked: string[];
  codexUnlocked: string[];
  storyFlags: string[];
  difficulty: DifficultyConfig;
  behaviorStats: PlayerBehaviorStats;
  dailiesCompleted: string[];  // 完成的每日挑战种子
  weekliesCompleted: string[]; // 完成的每周挑战 id
  settings: {
    musicVolume: number;
    sfxVolume: number;
    muted: boolean;
  };
}
```

#### 6.3.2 SaveManager 接口

```typescript
// src/game/save/SaveManager.ts
export class SaveManager {
  static KEY = "brother_save_v1";

  static load(): GameSave { /* ... */ }
  static save(save: GameSave): void { /* ... */ }
  static reset(): void { /* ... */ }
  static patch(save: GameSave): GameSave { /* ... */  /* 兼容旧版本 */ }

  // 便捷方法
  static setFlag(flag: string): void;
  static hasFlag(flag: string): boolean;
  static unlockCodex(category: CodexCategory, id: string): void;
  static updateBehavior(stats: Partial<PlayerBehaviorStats>): void;
  static setDifficulty(d: DifficultyConfig): void;
}
```

### 6.4 UI 草图

#### 6.4.1 HUD(扩展后)

```
┌────────────────────────────────────────────────────────────┐
│ [❤❤❤] 💰 1230    [火球糖 x5]    1-3 公交站音波  ⏸️  ❓     │  ← 顶部 HUD
│                                                            │
│  T:挑衅 [━━━━●━━] 5s    Y:大叫 [━━━━━●] 0s                │  ← 技能冷却(新增)
│                                                            │
│                                                            │
│                    [游戏画面]                               │
│                                                            │
│                                                            │
│                                                            │
│  💡 发现隐藏入口: 墙上好像有裂缝…                          │  ← 隐藏提示(底部)
└────────────────────────────────────────────────────────────┘
```

#### 6.4.2 道具合成提示

```
              ┌─────────────────────┐
              │   [A] + [B] = [AB]  │
              │                     │
              │    按 J 合成!       │
              │     (2.5s)         │
              │                     │
              │  不按就只保留 A    │
              └─────────────────────┘
```

#### 6.4.3 姐姐真心话界面

```
       屏幕渐暗(0.5s)
        ┌──────────────┐
        │   [姐姐立绘]  │
        │              │
        │   "弟弟…     │  ← 打字机效果
        │    其实姐姐…  │
        │    也不想追你"│
        │              │
        │   [继续]     │  ← 或自动 3s 后消失
        └──────────────┘
```

#### 6.4.4 主菜单(扩展后)

```
        ┌──────────────────────────────┐
        │   弟弟反击战                  │
        │                              │
        │   [开始游戏]                 │
        │   [继续游戏]                 │  ← 存档读取
        │   [图鉴]                     │  ← 新增
        │   [挑战模式]                 │  ← 新增(每日 / 每周)
        │   [难度调整]                 │  ← 新增
        │   [设置]                     │
        │   [关于]                     │
        └──────────────────────────────┘
```

#### 6.4.5 暂停菜单(扩展后)

```
   ┌────────────────────────────┐
   │   暂停                      │
   │                            │
   │   [继续]                    │
   │   [重新开始本关]            │
   │   [难度调整]                │  ← 新增
   │   [返回主菜单]              │
   │   [设置]                    │
   └────────────────────────────┘
```

### 6.5 资产扩展

#### 6.5.1 必需新增资源(汇总)

| 类别 | 资源 | 数量 | 优先级 | 备注 |
|---|---|---|---|---|
| 道具 | 5 个合成道具 512 图 | 5 | P0 | 燃烧爆米花、飞行泡泡、星跳冲刺、回旋飞锤、巨型冰砖 |
| 道具 | 5 个合成道具 128 HUD 图标 | 5 | P0 | 同上 |
| 音效 | 弟弟挑衅 / 大叫 / 受伤 台词 | 3 | P0 | voice_brother_taunt, _yell, _ouch |
| 音效 | 姐姐台词(傲娇、烦躁、暴怒、哭) | 5 | P0 | voice_sister_hmph, _wait, _mama, _cry, _defeat |
| 音效 | 姐姐警觉 | 1 | P0 | sfx_sister_aware |
| 音效 | 道具合成成功 | 1 | P0 | sfx_crafting_success |
| 图像 | 姐姐头部光圈 8 状态 | 8 | P1 | 64x64 PNG,可选 |
| 图像 | 章节小剧场背景 | 9 | P1 | 1920x1080 |
| 图像 | 弟弟 / 姐姐立绘姿势 | 4×2 | P1 | 站、跑、笑、生气 |
| 图像 | 姐姐日记配图 | 14 | P1 | 800x600 |
| 图像 | 道具合成粒子 | 1 | P0 | 简单圆形 + alpha 即可 |
| 图像 | 隐藏入口图标 | 6 | P1 | 128x128 |
| 图像 | 难度滑块 UI | 1 | P2 | 可用 Phaser Graphics |

#### 6.5.2 现有资源复用(汇总)

| 现有资源 | 升级后用途 |
|---|---|
| 火球糖 512 图 | 合成结果展示 |
| 玩具锤 512 图 | 合成结果展示 |
| 跳跳鞋 512 图 | 道具合成的"组合"展示 |
| 星星披风 512 图 | 同上 |
| 泡泡盾 512 图 | 同上 |
| 回旋玩具 512 图 | 同上 |
| 爆米花炸弹 512 图 | 同上 |
| 飞行帽 512 图 | 同上 |
| 冰淇淋枪 512 图 | 同上 |
| 变大饼干 512 图 | 同上 |
| 姐姐 4 形态 PNG | 情绪状态色 tint 即可,不需要新立绘 |
| 现有 BGM | 真心话界面用 theme_sister 变奏 |

#### 6.5.3 manifest 扩展

```json
// assets/items/items-manifest.json 新增字段
{
  "items": [
    {
      "id": "fireball_candy",
      "name": "火球糖",
      "image": "assets/items/png/fireball_candy.png",
      "hudIcon": "assets/items/icons-128/fireball_candy.png",
      "craftedFrom": null,            // ← 新增
      "canCraftInto": ["burning_popcorn", "flame_hammer"]  // ← 新增
    },
    {
      "id": "burning_popcorn",
      "name": "燃烧爆米花",
      "image": "assets/items/png/burning_popcorn.png",
      "hudIcon": "assets/items/icons-128/burning_popcorn.png",
      "craftedFrom": ["fireball_candy", "popcorn_bomb"],  // ← 新增
      "canCraftInto": null,
      "uses": 3,
      "action": "throw_burning_popcorn"
    }
  ]
}
```

```json
// assets/concepts/levels/level-maps-manifest.json 新增字段
{
  "levels": [
    {
      "id": "1_1",
      "name": "校门口开跑",
      "assetKey": "concept_level_01_home_tutorial",
      "file": "assets/concepts/levels/level-01-map-v1.png",
      "secrets": ["secret_1_1_memory"],   // ← 新增
      "interactives": ["light_1_1_01", "fridge_1_1_01"],  // ← 新增
      "monologuePool": ["see_homework", "see_sister"]   // ← 新增
    }
  ]
}
```

```json
// 新文件: assets/codex/codex-manifest.json
{
  "codex": {
    "items": ["fireball_candy", "toy_hammer", "..."],
    "enemies": ["sister_small", "sister_headphone", "..."],
    "levels": ["1_1", "1_2", "..."],
    "badges": ["first_clear", "no_damage", "..."],
    "skins": ["brother_default", "sister_default", "..."]
  }
}
```

```json
// 新文件: assets/codex/diary-manifest.json
{
  "diaries": [
    {
      "id": "diary_sister_small_01",
      "sisterId": "sister_small",
      "title": "姐姐的小本本",
      "content": "...",
      "art": "assets/codex/diary_sister_small_01.png"
    }
  ]
}
```

```json
// 新文件: assets/cinematics/cinematics-manifest.json
{
  "cinematics": [
    {
      "id": "ch1_prologue",
      "trigger": "chapter_start",
      "chapter": 1,
      "frames": ["ch1_prologue_01", "ch1_prologue_02"],
      "musicKey": "theme_brother"
    }
  ]
}
```

---

## 7. 开发里程碑

### 阶段 1:P0 基础(P0 全部完成)

**目标**:游戏核心互动性大幅提升。

#### 1.1 SaveManager + DifficultyConfig(1 周)

- [ ] 创建 `SaveManager`(load/save/patch/reset)
- [ ] 创建 `DifficultyConfig`(滑块 + 预设)
- [ ] 改造 `EnemyManager` / `ItemManager` / `PlayerController` 读取配置
- [ ] 在主菜单和暂停菜单加"难度"入口

#### 1.2 姐姐情绪系统(2 周)

- [ ] 创建 `SisterEmotion.ts` 状态机
- [ ] 创建 `SisterBubble.ts` UI 组件
- [ ] 创建 `dialogPool.ts` 对话池
- [ ] 改造 `EnemyManager` 接入状态机
- [ ] 接入气泡显示 / 淡出
- [ ] 准备 8 个音频文件

#### 1.3 道具合成系统(2 周)

- [ ] 创建 5 个合成道具的 manifest 条目
- [ ] 准备 5 × 2 = 10 张 PNG(512 + 128)
- [ ] 创建 `CraftingManager` 逻辑
- [ ] 创建 `CraftingPrompt` UI
- [ ] 实现 5 个 `action` 函数
- [ ] 实现合成动画(粒子 + 白闪)

#### 1.4 弟弟反击技能(1 周)

- [ ] 创建 `PlayerAbilities` 数据
- [ ] 在 `PlayerController` 中加入 T / Y 按键
- [ ] HUD 底部加技能冷却条
- [ ] 准备 2 个弟弟语音

**阶段 1 验收**:
- 姐姐被打会有反应(气泡 + 状态色 + 速度变化)
- 捡到能合成的道具会弹出提示
- 弟弟可以挑衅 / 大叫
- 难度可在菜单调整
- 进度会保存

---

### 阶段 2:P1 故事 + 隐藏(2-3 个月)

**目标**:游戏有故事,关卡有探索。

#### 2.1 姐姐日记 + 真心话(2 周)

- [ ] 创建 `HeartfeltLines.ts` 数据(14 篇)
- [ ] 创建 `DialogBubble.ts` UI
- [ ] 在敌人 / Boss 被打败时触发
- [ ] 准备 14 张日记配图

#### 2.2 弟弟独白(1 周)

- [ ] 创建 `BrotherMonologue.ts` 数据
- [ ] 在游戏中各事件点触发
- [ ] 复用 `DialogBubble`

#### 2.3 关卡间小剧场(3 周)

- [ ] 创建 `CinematicScene.ts` 通用场景
- [ ] 写 9 章开头 + 9 章结尾的剧本
- [ ] 准备 18 张小剧场背景 + 8 个立绘姿势
- [ ] 在每关开始 / 结束触发

#### 2.4 隐藏层与支线房间(3 周)

- [ ] 创建 `SecretLevel.ts` 数据
- [ ] 创建 `SecretScene.ts` 通用场景
- [ ] 实现 6 种入口检测
- [ ] 实现 3 种隐藏关卡类型(回忆 / 挑战 / 彩蛋)
- [ ] 在每关加 1-2 个隐藏入口

**阶段 2 验收**:
- 每关至少 1 个隐藏入口
- 姐姐被打败有真心话
- 弟弟有独白气泡
- 每章有开头 / 结尾小剧场
- 姐姐日记可在图鉴查看

---

### 阶段 3:P2 重玩(1-2 个月)

**目标**:游戏有重玩动力。

#### 3.1 收藏图鉴(3 周)

- [ ] 创建 `CodexEntry.ts` 数据
- [ ] 创建 `CodexStore.ts` 解锁追踪
- [ ] 创建 `CodexScene.ts` 图鉴场景
- [ ] 在游戏中记录首次使用 / 击败
- [ ] 准备 5 个分类的数据

#### 3.2 场景可互动对象(4 周)

- [ ] 创建 `InteractiveObject.ts` 通用基类
- [ ] 实现 6 种互动类型(灯 / 水 / 弹跳 / 平台 / 风 / 门)
- [ ] 实现暗灯系统 `LightSystem`
- [ ] 在每关加 2-3 个互动对象
- [ ] HUD 提示系统

#### 3.3 动态难度(2 周)

- [ ] 创建 `BehaviorTracker.ts`
- [ ] 在关键事件点更新数据
- [ ] 在关卡开始应用反制策略
- [ ] 设置页加"清除数据"按钮

**阶段 3 验收**:
- 图鉴可在主菜单查看
- 场景中家具可互动
- 玩 5 关后,姐姐行为有可见变化
- 难度滑块有 4 个预设

---

### 阶段 4:P3 持续运营(长期)

**目标**:游戏有"每天回来"的理由。

#### 4.1 每日挑战(3 周)

- [ ] 创建种子系统
- [ ] 创建 `DailyScene`
- [ ] 创建 `DailyStore`
- [ ] 每日生成新关卡
- [ ] 每日奖励 = 姐姐日记卡片

#### 4.2 每周挑战(3 周)

- [ ] 创建 5 关串联的迷你挑战
- [ ] 5 种限制条件轮换
- [ ] 奖励 = 稀有皮肤

#### 4.3 双人 / 异步接力(2 个月)

- [ ] 异步接力:姐姐玩家预设机关
- [ ] 本地双人:键盘分屏
- [ ] (可选)网络对战

---

### 总时间估算

| 阶段 | 周期 | 主要交付 |
|---|---|---|
| 阶段 1 P0 | 1-1.5 个月 | 姐姐情绪 + 道具合成 + 存档 + 难度 |
| 阶段 2 P1 | 2-3 个月 | 姐姐日记 + 弟弟独白 + 小剧场 + 隐藏层 |
| 阶段 3 P2 | 1-2 个月 | 图鉴 + 场景互动 + 动态难度 |
| 阶段 4 P3 | 3-6 个月 | 每日 / 每周 + 双人 |
| **总计** | **8-12 个月** | **完整升级版** |

---

## 8. 附录

### 8.1 配置开关(可关闭的升级项)

为了方便测试和回退,所有升级项都支持配置开关。

```typescript
// src/game/config/FeatureFlags.ts
export interface FeatureFlags {
  sisterEmotion: boolean;       // 默认 true
  itemCrafting: boolean;        // 默认 true
  playerAbilities: boolean;     // 默认 true
  interactiveObjects: boolean;  // 默认 true
  secretLevels: boolean;        // 默认 true
  dynamicDifficulty: boolean;   // 默认 true
  dailyChallenge: boolean;      // 默认 false
  weeklyChallenge: boolean;     // 默认 false
  codex: boolean;               // 默认 true
  cinematics: boolean;          // 默认 true
  heartfelts: boolean;          // 默认 true
  monologues: boolean;          // 默认 true
}

export const DEFAULT_FLAGS: FeatureFlags = {
  sisterEmotion: true,
  itemCrafting: true,
  playerAbilities: true,
  interactiveObjects: true,
  secretLevels: true,
  dynamicDifficulty: true,
  dailyChallenge: false,        // 暂不开放
  weeklyChallenge: false,       // 暂不开放
  codex: true,
  cinematics: true,
  heartfelts: true,
  monologues: true,
};
```

### 8.2 测试用例清单

#### 8.2.1 姐姐情绪系统

- [ ] 玩家攻击未命中 → 姐姐不应改变状态
- [ ] 玩家攻击命中 1 次 → 姐姐变傲娇,出气泡
- [ ] 玩家持续命中 4 次 → 姐姐变哭哭,停止攻击
- [ ] 玩家远离 5 秒 → 姐姐变 calm_down
- [ ] Boss 姐姐无 cry 状态,直接 defeat

#### 8.2.2 道具合成

- [ ] 玩家持有 A,捡到 B,A 和 B 在合成表中 → 弹出提示
- [ ] 玩家在 3 秒内按 J → 合成成功,A 和 B 消失,出现 AB
- [ ] 玩家在 3 秒内未按 J → 只保留 A,B 消失
- [ ] 玩家持有 A,捡到 C,C 不在合成表中 → 直接替换(原行为)
- [ ] 合成结果用完后 → 不应再触发合成提示

#### 8.2.3 弟弟反击

- [ ] 玩家按 T → 触发挑衅,周围姐姐加速 2 秒
- [ ] 玩家按 Y → 触发大叫,周围姐姐被推开
- [ ] 玩家在 5 秒内再次按 Y → 不应触发(冷却中)
- [ ] 玩家血量 1 颗心时 → 求饶自动触发

#### 8.2.4 存档

- [ ] 关闭浏览器再打开 → 进度保留
- [ ] 清除 localStorage → 重置为初始状态
- [ ] 调整难度后刷新 → 难度保留

#### 8.2.5 隐藏层

- [ ] 玩家用星星披风撞墙 → 进入隐藏关卡
- [ ] 隐藏关卡完成后 → 返回主关卡,奖励到账
- [ ] 同一条隐藏入口不能重复进入(本关卡内)

#### 8.2.6 每日 / 每周

- [ ] 同一天 2 次进入每日挑战 → 用相同种子
- [ ] 跨天后进入 → 用新种子
- [ ] 通关后排行榜更新

### 8.3 性能预算

| 指标 | 目标 | 备注 |
|---|---|---|
| 帧率 | 60 FPS(PC)/ 30 FPS(手机) | 大量姐姐情绪粒子时要保证 |
| 加载时间 | < 3s | 用 manifest 懒加载 |
| 内存占用 | < 200MB | 隐藏关卡场景按需加载 |
| 存档大小 | < 100KB | 用字符串压缩 |

### 8.4 兼容性

- **浏览器**:Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- **手机**:iOS 14+, Android 9+
- **分辨率**:1366x768(最低) → 4K
- **输入**:键盘 + 鼠标 + 触屏

### 8.5 参考资料

- 《游戏开发规划文档.md》(原文档)
- 《游戏开发实现补充.md》(MVP 实现)
- 《道具物品设计说明.md》(道具设计)
- 《声音设计规划.md》(音效设计)
- 《音乐设计方案.md》(音乐设计)
- 现有 manifest 文件集

---

## 9. 基础体验全面重建（v2 增补）

> **本章是 v2 的核心增补。** v1 文档解决了"游戏有什么",但未解决"玩起来爽不爽"。
> 以下诊断基于当前 `src/game/GameScene.ts` 与 `levelData.ts` 的真实实现。

### 9.1 现状缺口诊断

#### 9.1.1 v1 文档的 6 个盲区

| 盲区 | v1 文档做法 | 实际问题 | 后果 |
|---|---|---|---|
| 操作 | 写"不破坏原手感" | 仅有 coyote/buffer,无可变跳跃、无空中加速曲线、下砸弱 | 像网页 demo,不像动作游戏 |
| 画面 | 写"沿用现有素材" | 平台/子弹/金币用 `makeRect` 生成色块;背景单图无视差 | 粗糙、廉价、无沉浸 |
| 难度 | 写"难度滑块 + 姐姐懂你" | 实际只有 `getEnemyThrowScale()` 按关线性调 CD/速度;关内无 escalation | 第 5 关和第 9 关"感觉一样难" |
| 战斗 | 写"情绪系统" | 踩怪/投射物/Boss 无连击、无评分、无危机波 | 打怪无爽点,纯消耗 |
| 关卡 | 10 关概念丰富 | 实现全是"矩形平台 + 巡逻怪 + 终点 Boss",机关几乎未落地 | 10 关玩起来像换皮 |
| Boss | 多阶段写在规划里 | 实现 = 多血 + 快投射,无阶段切换、无 telegraph | Boss 战无聊且 unfair |

#### 9.1.2 当前代码中的关键证据

```typescript
// GameScene.ts — 平台/子弹/金币均为运行时生成的色块
makeRect("platform:wood", 64, 32, 0xb97845, 0xf6d19d);
makeRect("projectile:wave", 44, 24, 0xd589ff, 0xffffff);

// GameScene.ts — 难度仅与 levelIndex 线性相关
private getEnemyThrowScale() {
  const t = this.levelIndex / 9;
  return {
    cooldownMul: Phaser.Math.Linear(1.48, 0.76, t),
    speedMul: Phaser.Math.Linear(0.64, 1.2, t),
    // ...
  };
}
```

**结论:** 必须先做 **P0-A 基础体验重建**,再做 v1 的情绪/合成/剧情;否则是在粗糙原型上贴装饰。

---

### 9.2 操作手感精修

#### 9.2.1 设计目标

达到「 Celeste / Mario 级」的**基础平台手感**——玩家觉得"角色听我的",而不是"角色在飘"。

#### 9.2.2 移动参数表（建议值）

| 参数 | 当前 MVP | 升级目标 | 说明 |
|---|---:|---:|---|
| 地面加速度 | 即时满速 | **1800 px/s²** | 0→满速约 0.14s,有重量感 |
| 地面减速度 | 即时停 | **2200 px/s²** | 松键快速刹停,不滑步 |
| 空中水平加速 | ≈地面同速 | **地面 × 0.72** | 空中可控但弱于地面 |
| 最大地面速度 | 255 | **280** | 略提,配合加速鞋 |
| 跳跃初速度 | -560 固定 | **可变 -480 ~ -620** | 短按/长按区分 |
| Coyote Time | 120ms | **150ms** | 边缘容错 |
| Jump Buffer | 120ms | **180ms** | 提前按跳有效 |
| 最大下落速度 | 默认 | **720** | 防止无限加速 |
| 快速下落 | 无 | **按住 S 时 ×1.45** | 缩短滞空,提高节奏 |
| 下砸 | 弱判定 | **独立状态 + 0.15s 无敌 + 震波** | 见 §9.5 |

#### 9.2.3 可变跳跃实现

```typescript
// src/game/player/MovementController.ts
export class MovementController {
  private jumpHeld = false;
  private jumpCutMultiplier = 0.42;  // 松键时 Y 速度 ×0.42

  onJumpPress() {
    if (!this.canJump()) return;
    const holdMs = this.input.jumpHeldDuration;
    const t = Phaser.Math.Clamp(holdMs / 220, 0.35, 1);
    const vy = Phaser.Math.Linear(-480, -620, t);
    this.body.setVelocityY(vy);
    this.jumpHeld = true;
  }

  onJumpRelease() {
    if (this.body.velocity.y < 0) {
      this.body.setVelocityY(this.body.velocity.y * this.jumpCutMultiplier);
    }
    this.jumpHeld = false;
  }

  updateAirControl(cursors: InputState, dt: number) {
    const target = cursors.left ? -1 : cursors.right ? 1 : 0;
    const accel = this.grounded ? GROUND_ACCEL : AIR_ACCEL;
    // 平滑逼近目标速度,而非直接 setVelocityX
    this.body.velocity.x = Phaser.Math.Linear(
      this.body.velocity.x,
      target * MAX_SPEED,
      1 - Math.exp(-accel * dt / MAX_SPEED)
    );
  }
}
```

#### 9.2.4 落地反馈

- **硬落地**(下落速度 > 400): dust ×2 + `sfx_player_land` 低音 + 0.04s squash
- **软落地**: 普通 dust
- **完美落地**(接跳跃后 0.2s 内再跳): 金色粒子 + combo 计数 +1

#### 9.2.5 下砸升级

```
[空中] + [S/下] → 进入 STOMP 状态
  → Y 速度设为 900(快速下落)
  → 落地瞬间:
      - 0.15s 无敌
      - 半径 80px 震波(可破砖、可击晕姐姐 0.8s)
      - 屏幕微震 + 尘土环
      - 若踩中敌人 → 弹起 vy=-420(可链下一跳)
```

#### 9.2.6 触屏操作升级

| 问题 | 现状 | 升级 |
|---|---|---|
| 按钮小/位置固定 | 底部三按钮 | **可拖拽 + 半透明 + 80px 最小触控区** |
| 无法下砸 | 无下砸键 | 新增 **下砸按钮** 或 **双击跳跃区** |
| 道具难按 | 单按钮 | 道具键 **长按=合成提示,短按=使用** |
| 误触 | 无 | 跳跃/移动 **互斥 deadzone** |

#### 9.2.7 实现步骤

1. 从 `GameScene` 拆出 `MovementController.ts`
2. 实现可变跳跃 + 空中加速曲线 + 快速下落
3. 重做 `isStompingEnemy` → 独立 `StompSystem`
4. 触屏 UI 重构(`TouchControls.ts`)
5. **手感验收标准**: 连续 10 次平台跳跃,玩家主观"不飘";下砸 5 次有明确震感

---

### 9.3 画面与视觉表现升级

#### 9.3.1 设计目标

从「色块原型」升级到「统一卡通风格成品」——玩家第一眼觉得"这是个正经游戏"。

#### 9.3.2 视觉分层架构

```
Layer -3  远背景(天空/云)     scrollFactor 0.1  不碰撞
Layer -2  中背景(建筑/家具)   scrollFactor 0.35 不碰撞
Layer -1  近背景(装饰)       scrollFactor 0.6  不碰撞
Layer  0  游戏层(平台/tile)   scrollFactor 1.0  碰撞
Layer  1  玩法对象(敌人/道具) scrollFactor 1.0
Layer  2  前景粒子/粉尘      scrollFactor 1.0
Layer  3  角色               scrollFactor 1.0
Layer  4  UI / 气泡          scrollFactor 0
```

#### 9.3.3 平台 Tile 替换方案

**禁止**正式版继续使用 `makeRect` 作为可见平台。

| 平台类型 | 当前 | 升级资产 | 规格 |
|---|---|---|---|
| 地面 | 绿色矩形 | `tile_ground_grass` 9-slice | 64×64,可横向 repeat |
| 木平台 | 棕色矩形 | `tile_platform_wood` | 带钉边/木纹 |
| 弹跳垫 | 粉色矩形 | `tile_bounce_sofa` | 按下压缩动画帧 |
| 冰面 | 蓝色矩形 | `tile_ice` | 高光 + 滑行粒子 |
| 问号砖 | 黄色矩形 | `tile_question_block` | 3 帧: idle / hit / empty |

每关主题映射:

| 关卡主题 | 地面 tile | 平台 tile |
|---|---|---|
| 家庭 | 地毯/木地板 | 沙发/茶几/积木 |
| 校门 | 人行道砖 | 路沿/站牌 |
| 教室 | 地砖 | 课桌/椅子 |
| 梦境 | 星云 | 枕头云 |

#### 9.3.4 角色视觉规范

- **统一描边**: 所有角色外轮廓 2px 深色描边(或预渲染进 PNG)
- **阴影**: 保留 oval shadow,随跳跃高度缩放 alpha
- **受击**: 白闪 2 帧 + 红 tint 3 帧
- **动画**: MVP 用 sin 摇摆;升级拆 **spritesheet**:
  - 弟弟: idle(2) / run(4) / jump(1) / fall(1) / stomp(2) / hurt(2) / cry(3) / win(2)
  - 姐姐小怪: walk(4) / hit(1) / defeat(3)

#### 9.3.5 投射物与特效

| 对象 | 当前 | 升级 |
|---|---|---|
| 火球 | 橙色矩形 | 精灵 + 尾焰粒子 + 拖影 |
| 音波 | 紫色矩形 | 横向扩散波纹(3 帧动画) |
| 作业本 | 无独立图 | 旋转飞行 + 页角翻动 |
| 金币 | 黄圆 | 精灵 4 帧旋转 + 拾取弧线飞向 HUD |

#### 9.3.6 后处理(轻量)

```typescript
// 可选: Phaser PostFX 或 canvas overlay
export const VISUAL_PRESET = {
  vignette: { strength: 0.12 },       // 四角暗角,聚焦角色
  bloom: { threshold: 0.85 },         // 高光物体(金币/道具)微发光
  colorGrade: { sat: 1.08, contrast: 1.05 },  // 整体更鲜艳
};
```

移动端可关闭 bloom 保性能。

#### 9.3.7 实现步骤

1. 美术产出 4 套 tile set(家庭/校外/室内/梦境),每套 8-12 张
2. `PlatformRenderer.ts`: 按 `RectSpec` 铺 tile,替代 `makeRect`
3. `ParallaxBackground.ts`: 每关 3 层视差
4. 角色 spritesheet 接入(可先 4 帧 run 循环)
5. 投射物精灵替换 + 粒子
6. **画面验收标准**: 截图不发"程序员美术"吐槽;平台能认出是什么家具

---

### 9.4 难度叠加系统

#### 9.4.1 设计目标

难度不是「第 N 关数字更大」,而是**同一关内、同一局内威胁层层叠加**,让玩家感到压力在攀升。

#### 9.4.2 四层难度模型

```
┌─────────────────────────────────────────────────┐
│  Layer 4  元难度 (玩家设置: 生命/滑块/预设)        │
├─────────────────────────────────────────────────┤
│  Layer 3  关卡难度 (关卡配置: 坑距/敌人密度)      │
├─────────────────────────────────────────────────┤
│  Layer 2  段落难度 (单关内 A→F 段递升)            │
├─────────────────────────────────────────────────┤
│  Layer 1  局内 Escalation (时间/连击/危机波)     │  ← MVP 完全缺失
└─────────────────────────────────────────────────┘
```

**四层相乘**,而非只调 Layer 3。

#### 9.4.3 Layer 1: 局内 Escalation（核心新增）

```typescript
// src/game/difficulty/ThreatEscalation.ts
export interface ThreatState {
  level: number;           // 0-5, 局内威胁等级
  timer: number;         // 距上次升级秒数
  comboStreak: number;   // 连击维持
  playerHp: number;      // 残血加速
}

export class ThreatEscalation {
  private state: ThreatState = { level: 0, timer: 0, comboStreak: 0, playerHp: 3 };

  update(dt: number, ctx: GameContext) {
    this.state.timer += dt;

    // 规则 1: 每 45 秒未通关,威胁 +1
    if (this.state.timer > 45) {
      this.bump("time_pressure");
      this.state.timer = 0;
    }

    // 规则 2: 连击 ≥8 维持 5 秒,姐姐进入"认真模式"(投射加速)
    if (ctx.combo >= 8) this.bump("player_domination");

    // 规则 3: 玩家 1 心,敌人攻击频率 +25%(给残血压迫,非惩罚)
    if (ctx.playerHp === 1) this.applyModifier("low_hp_rush", 1.25);

    // 规则 4: 进入 Boss 区,威胁直接设为 3
    if (ctx.inBossZone) this.state.level = Math.max(this.state.level, 3);
  }

  bump(reason: string) {
    if (this.state.level >= 5) return;
    this.state.level++;
    this.broadcast(`threat_up:${reason}`);
    // 触发: 额外刷 1 小怪 / 投射 CD×0.85 / 屏幕边缘变红
  }

  getModifiers() {
    const l = this.state.level;
    return {
      enemySpeedMul: 1 + l * 0.06,
      throwCooldownMul: 1 - l * 0.08,
      extraProjectiles: l >= 3 ? 1 : 0,
      screenTint: l >= 4 ? 0xff0000 : 0xffffff,
      screenTintAlpha: l >= 4 ? 0.06 : 0,
    };
  }
}
```

#### 9.4.4 Layer 2: 单关段落压力曲线

每关按原规划 §8.1 分为 A-F 段,**每段有独立 `segmentThreat` 配置**:

```typescript
export interface LevelSegment {
  id: string;                    // "A" | "B" | ... | "F"
  xStart: number;
  xEnd: number;
  threatBase: number;            // 0-3
  spawnBudget: number;           // 本段最多同时活跃敌人数
  projectileBudget: number;      // 本段最多同屏投射物
  mechanics: string[];           // ["first_gap", "headphone_intro"]
}

// 示例: 第 3 关
const level3Segments: LevelSegment[] = [
  { id: "A", xStart: 0,    xEnd: 660,  threatBase: 0, spawnBudget: 1, projectileBudget: 2, mechanics: [] },
  { id: "B", xStart: 660,  xEnd: 1320, threatBase: 1, spawnBudget: 2, projectileBudget: 3, mechanics: ["narrow_pad"] },
  { id: "C", xStart: 1320, xEnd: 2200, threatBase: 2, spawnBudget: 3, projectileBudget: 4, mechanics: ["wave_dodge"] },
  { id: "D", xStart: 2200, xEnd: 2800, threatBase: 1, spawnBudget: 1, projectileBudget: 2, mechanics: ["checkpoint"] },
  { id: "E", xStart: 2800, xEnd: 3600, threatBase: 3, spawnBudget: 4, projectileBudget: 5, mechanics: ["combo_platforms"] },
  { id: "F", xStart: 3600, xEnd: 9999, threatBase: 2, spawnBudget: 2, projectileBudget: 3, mechanics: ["boss_gate"] },
];
```

**段落切换时**:
- 短暂 BGM 变奏(加一层鼓)
- HUD 显示段名(可选)
- 威胁等级不低于 `segment.threatBase`

#### 9.4.5 Layer 3: 危机波次（Crisis Wave）

每关 D→E 段之间,触发 **1 次危机波**:

```
[屏幕边缘闪红 0.5s]
[BGM 切 fight_loop]
[15 秒内]:
  - 连续刷 3 波敌人(3s / 5s / 7s 间隔)
  - 投射物密度 ×1.5
  - 击败全部 → 大奖(问号砖×2 + 恢复 1 心)
[结束]:
  - BGM 回 gameplay
  - 威胁等级 -1(奖励喘息)
```

#### 9.4.6 Layer 4: 与玩家设置叠加

```typescript
finalDifficulty = {
  enemySpeed: baseSpeed * segmentMul * escalationMul * settings.enemySpeedMul,
  throwCD: baseCD * segmentMul * escalationMul / settings.enemySpeedMul,
  playerHp: settings.playerMaxHp,
};
```

#### 9.4.7 Boss 多阶段（可读难度）

| 阶段 | HP 阈值 | 行为 | Telegraph(预警) |
|---|---|---|---|
| P1 | 100%-60% | 单发作业本 | 举手 0.4s + 台词气泡 |
| P2 | 60%-30% | 双发 + 震地 | 跳起蓄力 0.6s + 地面裂纹 |
| P3 | 30%-0% | 召唤小怪 + 没收道具 | 屏幕变暗 + "没收!" 1.2s |

**Telegraph 规则**: 任何能扣血的技能,**至少 0.35s 视觉预警**。

#### 9.4.8 实现步骤

1. 创建 `ThreatEscalation.ts` + `LevelSegment.ts`
2. `levelData.ts` 每关加 `segments[]`
3. `EnemyManager` 读取 `getModifiers()` 叠加到 AI
4. 实现 `CrisisWaveController.ts`
5. 重写 `BossController` 为阶段状态机 + telegraph
6. **难度验收标准**: 同一关玩 3 次,第 3 次明显比第 1 次前 30 秒更紧张;Boss 技能能"看懂"

---

### 9.5 核心战斗与爽感循环

#### 9.5.1 设计目标

让「打姐姐」从 chores 变成 **score attack**——有连击、有评价、有回报。

#### 9.5.2 连击系统

```typescript
// src/game/combat/ComboSystem.ts
export class ComboSystem {
  private count = 0;
  private timer = 0;
  private readonly COMBO_WINDOW_MS = 2200;

  registerHit(type: "stomp" | "projectile" | "hammer" | "bomb") {
    this.count++;
    this.timer = this.COMBO_WINDOW_MS;
    const label = this.getLabel(this.count);
    if (label) this.showFloatingText(label, this.getColor(this.count));
    this.audio.playComboTier(this.count);
  }

  getLabel(n: number): string | null {
    if (n === 3) return "Good!";
    if (n === 5) return "Great!";
    if (n === 8) return "Fantastic!";
    if (n === 12) return "Perfect!";
    if (n >= 15) return "姐姐服了!";
    return null;
  }

  getCoinBonus(): number {
    return 1 + Math.floor(this.count / 5) * 0.1;  // 每 5 连击金币 +10%
  }
}
```

#### 9.5.3 关卡评分(三星)

| 星级 | 条件 |
|---|---|
| ★ | 通关 |
| ★★ | 通关 + 未哭过(无伤或仅 1 伤) + 时间 < 目标×1.3 |
| ★★★ | 通关 + 无伤 + 时间 < 目标×1.0 + 连击峰值 ≥8 |

结算界面显示:
- 通关时间 / 击败数 / 最高连击 / 收集率
- 姐姐一句吐槽("哼,下次我会更快!")

#### 9.5.4 道具博弈(单槽位的策略深度)

即使只持有 1 个道具,也要让玩家**纠结**:

| 情境 | 最优选择 | 设计意图 |
|---|---|---|
| 危机波次前 | 留泡泡盾 | Teaching moment |
| Boss P2 震地 | 留跳跳鞋躲震波 | 道具非仅输出 |
| 连击 7 快断 | 用火球续连击 | 连击与消耗博弈 |
| 隐藏入口在墙后 | 留星星披风 | 探索奖励 |

**UI 提示**: 道具栏旁显示"建议?"图标(可关),新手友好。

#### 9.5.5 敌人协同（组合威胁）

单独敌人简单,**组合才形成难度**:

| 组合 | 效果 | 首次出现关 |
|---|---|---|
| 耳机 + 小怪 | 音波逼跳,落地踩小怪 | L3 |
| 管道 + 气球 | 探头时气球投物 | L5 |
| 双耳机交叉波 | 左右夹击 | L7 危机波 |
| Boss + 小怪骚扰 | 打 Boss 时被小怪撞 | L10 P3 |

#### 9.5.6 实现步骤

1. `ComboSystem.ts` + 浮动文字 UI
2. 结算界面 `ResultScene.ts`(星级 + 统计)
3. `levelData.ts` 加 `targetTimeSec`
4. 敌人 spawn 表支持 `synergyGroups`
5. 道具 UI 加 contextual hint(可选)

---

### 9.6 关卡节奏与机制密度

#### 9.6.1 当前问题

10 关配置差异主要在**平台坐标**和**敌人数量**,缺少规划文档中的:
- 移动平台(购物车/黑板擦/秋千)
- 红绿灯机关
- 抽屉伸缩
- 镜子传送
- 限时滚石

#### 9.6.2 每关「机制名片」(必须落地 1 个独占机制)

| 关 | 独占机制 | 实现优先级 |
|---|---|---|
| L1 | 低台阶教学 + 弹跳沙发 | 已有 bounce,需视觉 |
| L2 | **移动斑马线平台** | P0-B |
| L3 | **音波预警线**(可见弹道) | P0-B |
| L4 | 连续弹跳垫链 | P0-B |
| L5 | **作业本雨**(定时弹幕) | P1 |
| L6 | **滚动物障碍** | P1 |
| L7 | **冰面滑行**(摩擦系数) | P0-B |
| L8 | **垂直泡泡塔**(限时平台) | P1 |
| L9 | **低重力区切换** | 已有 gravityY |
| L10 | **机制回忆混合 + 多阶段 Boss** | P0-B |

#### 9.6.3 机关 Controller 架构

```typescript
// src/game/mechanics/MechanicController.ts
export interface MechanicController {
  id: string;
  update(dt: number, ctx: GameContext): void;
  onPlayerEnter?(player: Player): void;
  reset(): void;
}

// 示例: 移动平台
export class MovingPlatformController implements MechanicController {
  id = "moving_platform";
  update(dt, ctx) {
    this.platforms.forEach(p => {
      p.x += p.speed * p.direction * dt;
      if (p.x < p.range[0] || p.x > p.range[1]) p.direction *= -1;
    });
  }
}
```

每关 `levelData` 增加:

```typescript
mechanics: [
  { type: "moving_platform", platforms: [{ ref: "plat_3", range: [1200, 1450], speed: 90 }] },
  { type: "projectile_rain", segment: "E", intervalMs: 1800, projectile: "homework" },
];
```

#### 9.6.4 「每 15 秒一个刺激」检查清单

关卡设计完成后,用 timeline 工具或表格验证:

| 时间 | 刺激类型 | 内容 |
|---|---|---|
| 0-15s | 教学 | 第一次跳跃 + 金币弧 |
| 15-30s | 战斗 | 第一个姐姐 + 踩头 |
| 30-45s | 收集 | 问号砖 + 道具 |
| 45-60s | 挑战 | 第一个坑/音波 |
| 60-75s | 奖励 | 检查点 + 回血道具 |
| ... | ... | ... |

**未通过检查的关卡不得合并进主分支。**

---

### 9.7 Juice 与反馈系统

#### 9.7.1 设计目标

同一操作,MVP 只有音效;升级后要有**视觉 + 听觉 + 触觉(触屏震动) + 时间(顿帧)** 四层反馈。

#### 9.7.2 Juice 事件表

| 事件 | 视觉 | 音频 | 时间 | 镜头 |
|---|---|---|---|---|
| 跳跃 | 尘土 puffs | jump 变体 pitch | - | - |
| 落地(硬) | squash 0.08s | land 低音 | - | - |
| 踩怪 | 星星爆发 + 敌人 flatten | stomp + 姐姐 hit | **hitstop 40ms** | shake 0.003 |
| 受伤 | 红闪 + 星星绕头 | hurt + voice | hitstop 60ms | shake 0.008 |
| 捡金币 | 弧线飞向 HUD | pitch 随连击升高 | - | - |
| 捡道具 | 光环扩散 | item_pickup | freeze 80ms | zoom 1.02 0.1s |
| 合成成功 | 白闪 + 粒子 | craft_success | freeze 120ms | zoom 1.05 |
| 危机波开始 | 边缘红 vignette | fight BGM 切入 | - | zoom 0.98 |
| Boss 阶段切换 | 全屏闪 + 台词 | sister theme 变奏 | slowmo 0.3s | shake |
| 通关 |  confetti + 弟弟 win pose | victory jingle | - | zoom 角色 |

#### 9.7.3 Hitstop 实现

```typescript
// src/game/juice/Hitstop.ts
export function hitstop(scene: Phaser.Scene, ms: number) {
  const prev = scene.physics.world.timeScale;
  scene.physics.world.timeScale = 0.05;
  scene.time.delayedCall(ms, () => {
    scene.physics.world.timeScale = prev;
  });
}
```

#### 9.7.4 音效分层

```typescript
// AudioManager 扩展
playSfx(key, { pitch = 1, volume = 1, bus = "sfx" });
playComboTier(combo: number) {
  const pitch = 1 + Math.min(combo, 15) * 0.03;
  this.playSfx("sfx_coin_pickup", { pitch });
}
```

同类音效至少 3 变体,随机播放防疲劳。

#### 9.7.5 触屏震动(可选)

```typescript
if (navigator.vibrate) {
  navigator.vibrate({ stomp: 15, hurt: [30, 20, 30], bossPhase: 50 });
}
```

#### 9.7.6 实现步骤

1. 创建 `src/game/juice/JuiceDirector.ts` 统一调度
2. 创建 `Hitstop.ts` / `ScreenFX.ts`(shake/flash/zoom)
3. 所有 combat 事件改走 `JuiceDirector.emit("stomp", ctx)`
4. 扩展 `AudioManager` 变体 + pitch
5. **Juice 验收标准**: 录屏 30 秒,非开发者愿意转发到群聊

---

### 9.8 修订后的总优先级与里程碑

#### 9.8.1 开发 waves（取代原 §7 阶段排序）

```
Wave 0 — 基础体验 (4-6 周) ← 必须先做
  ├─ MovementController + 可变跳跃 + 下砸
  ├─ Platform tile 替换 + 视差背景
  ├─ ThreatEscalation + LevelSegment
  ├─ ComboSystem + ResultScene 星级
  ├─ JuiceDirector + Boss telegraph
  └─ 触屏控件重构

Wave 1 — 内容与深度 (4-6 周)
  ├─ 姐姐情绪 + 道具合成 (v1 P0)
  ├─ 5 个独占机关 controller
  ├─ 危机波次 + 敌人协同
  ├─ SaveManager + 图鉴基础
  └─ 隐藏层 ×3 关试点

Wave 2 — 情感与运营 (6-8 周)
  ├─ 小剧场 + 真心话 + 独白
  ├─ 每日挑战
  ├─ 难度滑块 + 动态 AI
  └─ 完整 10 关机制落地

Wave 3 — 扩展
  └─ 双人 / 关卡编辑器 / 更多皮肤
```

#### 9.8.2 Wave 0 验收清单（Definition of Done）

- [ ] 连续跳跃 10 次不飘,可变跳跃可感知
- [ ] 平台不是色块,至少 2 套主题 tile
- [ ] 背景有 2 层以上视差
- [ ] 单关内 45 秒后敌人明显更 aggressive
- [ ] 踩怪有 hitstop + 星星 + 连击字
- [ ] Boss 每个技能有 ≥0.35s 预警
- [ ] 通关结算有星级 + 统计
- [ ] 触屏下砸可用
- [ ] 帧率 PC 60 / 手机 30 稳定

**Wave 0 未通过,不启动 Wave 1 的剧情/合成大开发。**

#### 9.8.3 修订后的 FeatureFlags 默认值

```typescript
export const DEFAULT_FLAGS: FeatureFlags = {
  // Wave 0 — 默认全开
  movementOverhaul: true,
  threatEscalation: true,
  comboSystem: true,
  juiceDirector: true,
  tilePlatforms: true,
  parallaxBackground: true,
  bossPhases: true,
  touchControlsV2: true,

  // Wave 1
  sisterEmotion: true,
  itemCrafting: true,
  crisisWaves: true,
  secretLevels: true,

  // Wave 2+
  cinematics: true,
  dailyChallenge: false,
  dynamicDifficulty: false,
};
```

---

## 结语

本次升级 v2 的核心思路:**先让游戏"好玩、好控、好看、有压力",再让游戏"有故事、有收集、有理由回来"。**

v1 文档偏**内容装饰层**(情绪、合成、日记)——重要,但无法拯救粗糙的原型。
v2 增补的 **Wave 0 基础体验重建** 才是解决「普通、粗糙、不好玩、难度不叠加」的根因。

| 玩家感受 | 靠什么解决 |
|---|---|
| "操作粘/飘" | §9.2 MovementController |
| "画面像 demo" | §9.3 Tile + 视差 + 精灵 |
| "越玩越一样" | §9.4 ThreatEscalation + 段落 + 危机波 |
| "打怪没爽感" | §9.5 连击 + 星级 + 道具博弈 |
| "10 关换皮" | §9.6 独占机制 + 15 秒刺激检查 |
| "打了没反馈" | §9.7 JuiceDirector |
| "想再玩一次" | v1 图鉴/每日/剧情(Wave 1-2) |

> 升级完成后,《弟弟反击战》应首先是一款**手感扎实、画面可爱、难度有层次**的横版动作小品;
> 其次才是一个**有姐弟温度**的故事体验。

---

**版本**:v2.0
**适用项目**:`姐弟游戏`(《弟弟反击战》)
**基础**:`Phaser 3 + Vite + TypeScript`
**日期**:2026-06-19
**变更**:v2 增补 §9 基础体验全面重建;修订 §1-2 优先级与原则
